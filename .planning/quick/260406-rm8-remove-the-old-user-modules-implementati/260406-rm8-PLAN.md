---
phase: quick-260406-rm8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/components/sidebar.tsx
  - web-ui/app/app/admin/users/page.tsx
  - web-ui/app/app/admin/layout.tsx
  - web-ui/app/api/admin/users/route.ts
  - web-ui/app/api/admin/users/role/route.ts
autonomous: true
requirements: [RM8-01]

must_haves:
  truths:
    - "Sidebar 'Users' link navigates to /app/settings/members (RBAC-based)"
    - "No Cognito ListUsersCommand call exists in any API route"
    - "Old /app/admin/users page and /api/admin/users routes are deleted"
  artifacts:
    - path: "web-ui/components/sidebar.tsx"
      provides: "Updated nav link pointing to /app/settings/members"
  key_links:
    - from: "web-ui/components/sidebar.tsx"
      to: "/app/settings/members"
      via: "href update on Users nav item"
      pattern: "settings/members"
---

<objective>
Remove the old Cognito-based user management module and redirect the sidebar nav link to the existing RBAC-based members page.

Purpose: The platform migrated to a full RBAC system with Prisma-backed member management at /app/settings/members. The old /app/admin/users page still calls Cognito ListUsersCommand and the old /api/admin/users routes are dead weight.
Output: Old files deleted, sidebar updated, no Cognito user listing code remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old Cognito user module files</name>
  <files>
    web-ui/app/app/admin/users/page.tsx,
    web-ui/app/app/admin/layout.tsx,
    web-ui/app/api/admin/users/route.ts,
    web-ui/app/api/admin/users/role/route.ts
  </files>
  <action>
    Delete all four files. The /app/app/admin/ directory only contains the users page and its layout — both can go.

    - web-ui/app/app/admin/users/page.tsx — old Cognito-based UserManagementPage component
    - web-ui/app/app/admin/layout.tsx — layout that only wraps the users page (no other pages in /admin/)
    - web-ui/app/api/admin/users/route.ts — GET handler calling Cognito ListUsersCommand
    - web-ui/app/api/admin/users/role/route.ts — POST handler for old role assignment via Cognito sub

    Use `rm -rf` on the entire web-ui/app/app/admin/ directory and web-ui/app/api/admin/ directory since both are exclusively used by the old module.
  </action>
  <verify>
    ls web-ui/app/app/admin/ 2>&1 | grep "No such file" &&
    ls web-ui/app/api/admin/ 2>&1 | grep "No such file"
  </verify>
  <done>Both directories no longer exist; no Cognito user listing code remains in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar Users link to settings/members</name>
  <files>web-ui/components/sidebar.tsx</files>
  <action>
    In web-ui/components/sidebar.tsx, find the nav item for "Users" (currently at line ~51):

    ```
    { name: "Users", href: "/app/admin/users", icon: Users },
    ```

    Change the href to point to the existing RBAC-based members page:

    ```
    { name: "Users", href: "/app/settings/members", icon: Users },
    ```

    No other changes needed — the label "Users" and icon stay the same.
  </action>
  <verify>
    grep "settings/members" web-ui/components/sidebar.tsx &&
    ! grep "admin/users" web-ui/components/sidebar.tsx
  </verify>
  <done>Sidebar Users link points to /app/settings/members; no reference to /app/admin/users remains.</done>
</task>

</tasks>

<verification>
cd web-ui && npm run build 2>&1 | tail -20
</verification>

<success_criteria>
- No files under web-ui/app/app/admin/ or web-ui/app/api/admin/
- Sidebar Users href is /app/settings/members
- No grep hits for "admin/users" anywhere in web-ui/
- Build passes with no new errors
</success_criteria>

<output>
After completion, create .planning/quick/260406-rm8-remove-the-old-user-modules-implementati/260406-rm8-SUMMARY.md
</output>
