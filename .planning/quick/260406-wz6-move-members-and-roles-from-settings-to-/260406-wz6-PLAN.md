---
phase: quick
plan: 260406-wz6
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/app/app/users-permissions/layout.tsx
  - web-ui/app/app/users-permissions/users/page.tsx
  - web-ui/app/app/users-permissions/roles/page.tsx
  - web-ui/components/sidebar.tsx
  - web-ui/app/app/settings/page.tsx
autonomous: true
requirements: [QUICK]
must_haves:
  truths:
    - "Sidebar shows collapsible 'Users & Permissions' section with 'Users' and 'Roles & Permissions' sub-items"
    - "Navigating to /app/users-permissions/users shows the Members page content (team members + invitations)"
    - "Navigating to /app/users-permissions/roles shows the Roles page content (predefined + custom roles)"
    - "Settings page no longer has Members or Roles tabs"
    - "Old sidebar 'Users' link pointing to /app/settings/members is replaced by the new section"
  artifacts:
    - path: "web-ui/app/app/users-permissions/users/page.tsx"
      provides: "Users page (moved from settings/members)"
    - path: "web-ui/app/app/users-permissions/roles/page.tsx"
      provides: "Roles & Permissions page (moved from settings/roles)"
    - path: "web-ui/app/app/users-permissions/layout.tsx"
      provides: "Auth guard layout for users-permissions section"
  key_links:
    - from: "web-ui/components/sidebar.tsx"
      to: "/app/users-permissions/users, /app/users-permissions/roles"
      via: "collapsible nav section with sub-links"
      pattern: "users-permissions"
---

<objective>
Move Members and Roles out of the Settings module into a dedicated "Users & Permissions" sidebar section with collapsible sub-navigation.

Purpose: Give user/role management first-class visibility instead of burying it under Settings tabs.
Output: New /app/users-permissions/ routes, updated sidebar with collapsible section, cleaned-up Settings page.
</objective>

<context>
@web-ui/components/sidebar.tsx
@web-ui/app/app/settings/page.tsx
@web-ui/app/app/settings/members/page.tsx
@web-ui/app/app/settings/roles/page.tsx
@web-ui/app/app/settings/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /app/users-permissions routes and move page content</name>
  <files>
    web-ui/app/app/users-permissions/layout.tsx
    web-ui/app/app/users-permissions/users/page.tsx
    web-ui/app/app/users-permissions/roles/page.tsx
  </files>
  <action>
    1. Create `web-ui/app/app/users-permissions/layout.tsx` — server component with `requireAuth('read', 'Account')` guard (same pattern as settings/layout.tsx).

    2. Create `web-ui/app/app/users-permissions/users/page.tsx` — move the full content from `web-ui/app/app/settings/members/page.tsx` here. Update the page heading from "Members" to "Users". Keep all existing functionality (team members table, pending invitations, invite dialog, role change). All API calls stay the same (/api/settings/members, /api/invitations, /api/settings/roles).

    3. Create `web-ui/app/app/users-permissions/roles/page.tsx` — move the full content from `web-ui/app/app/settings/roles/page.tsx` here. Update the page heading from "Roles" to "Roles & Permissions". Keep all existing functionality (predefined roles, custom roles, create/edit/delete dialogs).

    4. Delete the old route files:
       - `web-ui/app/app/settings/members/page.tsx`
       - `web-ui/app/app/settings/roles/page.tsx`
       - Remove the `members/` and `roles/` directories under settings if empty.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>New routes exist at /app/users-permissions/users and /app/users-permissions/roles with moved page content. Old settings/members and settings/roles routes deleted. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar with collapsible Users & Permissions section and clean Settings page</name>
  <files>
    web-ui/components/sidebar.tsx
    web-ui/app/app/settings/page.tsx
  </files>
  <action>
    1. In `web-ui/components/sidebar.tsx`:
       - Import `ChevronDown` from lucide-react (already have ChevronLeft/Right).
       - Import `Shield` from lucide-react for the Roles & Permissions sub-item.
       - Replace the flat `{ name: "Users", href: "/app/settings/members", icon: Users }` entry with a collapsible section object.
       - Add a `usersPermissionsOpen` state (boolean, default to true when pathname starts with `/app/users-permissions`).
       - In the nav rendering, detect the collapsible section and render:
         - A parent button "Users & Permissions" with Users icon + ChevronDown chevron (rotates when collapsed).
         - When expanded, show two indented sub-links:
           - "Users" → `/app/users-permissions/users` (icon: Users)
           - "Roles & Permissions" → `/app/users-permissions/roles` (icon: Shield)
       - The parent button is active (highlighted) if pathname starts with `/app/users-permissions`.
       - Sub-items use slightly smaller text and left padding (pl-8 or pl-10) for visual hierarchy.
       - When sidebar is collapsed (collapsed=true), show only the Users icon for the parent; clicking it navigates to `/app/users-permissions/users` directly (no expand/collapse in collapsed mode).
       - Place the section between "Audit Logs" and "Settings" in the navigation order (same position as the old "Users" item).

    2. In `web-ui/app/app/settings/page.tsx`:
       - Remove the "Roles" TabsTrigger and "Members" TabsTrigger (lines ~39-63 area).
       - Remove the `Users` import from lucide-react if no longer used.
       - Keep: Appearance, Profile, Notifications, Security, Organization tabs.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Sidebar shows collapsible "Users & Permissions" section with two sub-items. Settings page only has Appearance, Profile, Notifications, Security, and Organization tabs. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` — zero errors
- `cd web-ui && npm run lint` — no new lint errors
- Visual: sidebar shows "Users & Permissions" as collapsible with "Users" and "Roles & Permissions" sub-items
- Visual: Settings page no longer shows Members or Roles tabs
- Navigation: /app/users-permissions/users renders the members/invitations content
- Navigation: /app/users-permissions/roles renders the roles management content
</verification>

<success_criteria>
Members and Roles are accessible from a dedicated "Users & Permissions" collapsible sidebar section at new routes. Settings page is cleaned up. All existing functionality preserved — only the navigation structure changed.
</success_criteria>

<output>
After completion, create `.planning/quick/260406-wz6-move-members-and-roles-from-settings-to-/260406-wz6-SUMMARY.md`
</output>
