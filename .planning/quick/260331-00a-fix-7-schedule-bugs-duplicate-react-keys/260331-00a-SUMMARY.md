# Quick Task 260331-00a: Fix 7 Schedule Bugs — Summary

**Completed:** 2026-03-31

## Changes

### Task 1: Repository layer + API validation (BUG-2, BUG-3, BUG-4, BUG-5)

**Files modified:**
- `web-ui/lib/db/repositories/schedule/postgres.ts`
- `web-ui/app/api/schedules/route.ts`

**What changed:**
- **BUG-2 (resourceTypes dedup):** `transformToUISchedule` now wraps resourceTypes in `[...new Set()]` — API returns unique types like `["ec2","ecs"]` instead of duplicates
- **BUG-3 (day ordering):** Days sorted by calendar order (Mon→Sun) using `DAY_ORDER` lookup before returning from repository
- **BUG-4 (nextExecution):** Added `computeNextExecution()` helper that finds the next matching day+starttime within 7 days; active schedules now show computed next execution dates
- **BUG-5 (time validation):** POST `/api/schedules` now rejects `starttime === endtime` with 400 error

### Task 2: React keys, stats cards, E2E test (BUG-1, BUG-6, BUG-7)

**Files modified:**
- `web-ui/components/schedules/schedules-grid.tsx`
- `web-ui/components/schedules/schedules-table.tsx`
- `web-ui/components/schedules/schedules-list.tsx`
- `web-ui/components/schedules/schedule-details-dialog.tsx`
- `web-ui/components/schedules/schedules-client-component.tsx`
- `tests/e2e/schedules-pg.spec.ts`

**What changed:**
- **BUG-1 (duplicate keys):** All 4 components now use `key={`${type}-${index}`}` as defense-in-depth (root cause fixed by BUG-2 dedup)
- **BUG-6 (stats cards):** Stats cards now derive from filtered `schedules` when filters are active, falling back to `allSchedules` when no filters applied
- **BUG-7 (E2E test):** Fixed test to set up request interception before dropdown interaction and click "Apply Filters" button before awaiting the API call

## Test Results

- 12/12 postgres repository tests pass
- 32/32 schedules API route tests pass
