---
phase: quick
plan: 260331-00a
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/lib/db/repositories/schedule/postgres.ts
  - web-ui/components/schedules/schedules-grid.tsx
  - web-ui/components/schedules/schedules-table.tsx
  - web-ui/components/schedules/schedules-list.tsx
  - web-ui/components/schedules/schedule-details-dialog.tsx
  - web-ui/app/api/schedules/route.ts
  - tests/e2e/schedules-pg.spec.ts
autonomous: true
must_haves:
  truths:
    - "No duplicate React key warnings in browser console on schedules page"
    - "resourceTypes array contains unique values from API response"
    - "Days display in Mon-Sun calendar order in all views"
    - "nextExecution shows a computed date for active schedules with valid config"
    - "POST /api/schedules rejects starttime === endtime with 400"
    - "Stats cards reflect filtered data when filters are active"
    - "E2E status filter test passes without timeout"
  artifacts:
    - path: "web-ui/lib/db/repositories/schedule/postgres.ts"
      provides: "Deduplicated resourceTypes + nextExecution computation"
    - path: "web-ui/app/api/schedules/route.ts"
      provides: "starttime !== endtime validation"
    - path: "tests/e2e/schedules-pg.spec.ts"
      provides: "Fixed status filter E2E test"
  key_links:
    - from: "web-ui/lib/db/repositories/schedule/postgres.ts"
      to: "web-ui/components/schedules/*"
      via: "API response with deduplicated resourceTypes and computed nextExecution"
      pattern: "resourceTypes.*filter.*Boolean"
---

<objective>
Fix 7 schedule module bugs: duplicate React keys from non-unique resourceTypes, missing day ordering, null nextExecution, missing start/end time validation, stats cards ignoring filters, and broken E2E test.

Purpose: Eliminate 180+ console errors, fix data quality issues, and restore E2E test reliability.
Output: All 7 bugs fixed with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@web-ui/lib/db/repositories/schedule/postgres.ts
@web-ui/lib/types.ts
@web-ui/components/schedules/schedules-grid.tsx
@web-ui/components/schedules/schedules-table.tsx
@web-ui/components/schedules/schedules-list.tsx
@web-ui/components/schedules/schedule-details-dialog.tsx
@web-ui/components/schedules/schedules-client-component.tsx
@web-ui/components/schedules/schedules-client-component-api.tsx
@web-ui/app/api/schedules/route.ts
@web-ui/app/app/schedules/page.tsx
@tests/e2e/schedules-pg.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix repository layer (BUG-2, BUG-3, BUG-4) and API validation (BUG-5)</name>
  <files>web-ui/lib/db/repositories/schedule/postgres.ts, web-ui/app/api/schedules/route.ts</files>
  <action>
**BUG-2 + BUG-1 root cause — Deduplicate resourceTypes in postgres.ts `transformToUISchedule`:**
Change line 226 from:
```typescript
resourceTypes: resources.map((r) => r.type).filter(Boolean),
```
to:
```typescript
resourceTypes: [...new Set(resources.map((r) => r.type).filter(Boolean))],
```
This fixes BUG-2 (API returns unique types) and BUG-1 (no more duplicate keys since data is unique).

**BUG-3 — Sort days in calendar order in `transformToUISchedule`:**
Add a day-order constant at the top of the file:
```typescript
const DAY_ORDER: Record<string, number> = { Mon: 0, Tue: 1, Wed: 2, Thu: 3, Fri: 4, Sat: 5, Sun: 6 };
```
Change the `days` mapping in `transformToUISchedule` to sort:
```typescript
days: [...record.days].sort((a, b) => (DAY_ORDER[a] ?? 99) - (DAY_ORDER[b] ?? 99)),
```

**BUG-4 — Compute nextExecution in `transformToUISchedule`:**
Add a helper function `computeNextExecution(days: string[], starttime: string, timezone: string, active: boolean): string | undefined` that:
1. Returns undefined if `!active` or days is empty.
2. Gets current time in the schedule's timezone using `Intl.DateTimeFormat`.
3. Iterates from today through the next 7 days, checking if the day name (Mon/Tue/etc.) is in the schedule's `days` array.
4. For each matching day, constructs a Date from that day + starttime in the schedule timezone.
5. If that datetime is in the future, returns its ISO string.
6. If no future execution found in 7 days, returns undefined.
Set `nextExecution` in the return object using this helper.

**BUG-5 — Add starttime !== endtime validation in route.ts POST handler:**
After the existing `if (!body.name || ...)` validation block (around line 80), add:
```typescript
if (body.starttime === body.endtime) {
    return NextResponse.json(
        { success: false, error: 'Start time and end time must be different' },
        { status: 400 }
    );
}
```
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/web-ui && npx vitest run --reporter=verbose lib/db/repositories/schedule/postgres.test.ts app/api/schedules/schedules-api.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>resourceTypes are deduplicated, days sorted Mon-Sun, nextExecution computed for active schedules, starttime===endtime rejected with 400</done>
</task>

<task type="auto">
  <name>Task 2: Fix React key warnings (BUG-1 defense-in-depth), stats cards (BUG-6), and E2E test (BUG-7)</name>
  <files>web-ui/components/schedules/schedules-grid.tsx, web-ui/components/schedules/schedules-table.tsx, web-ui/components/schedules/schedules-list.tsx, web-ui/components/schedules/schedule-details-dialog.tsx, web-ui/components/schedules/schedules-client-component-api.tsx, tests/e2e/schedules-pg.spec.ts</files>
  <action>
**BUG-1 defense-in-depth — Fix React keys in all 4 components:**
Even though Task 1 deduplicates at the source, add index-based keys as safety net in case other repositories (DynamoDB) still return duplicates.

In each of these files, change `key={type}` to `key={`${type}-${index}`}` in the resourceTypes `.map()` call:
- `schedules-grid.tsx` line 249: `.map((type: string, index: number) =>` with `key={`${type}-${index}`}`
- `schedules-table.tsx` line 241: same pattern
- `schedules-list.tsx` line 218: same pattern
- `schedule-details-dialog.tsx` line 282: same pattern

**BUG-6 — Stats cards should reflect filtered view in schedules-client-component-api.tsx:**
This component uses `schedules` state (which is the full unfiltered API response) for stats. The `filteredSchedules` variable holds the client-filtered subset. The stats cards are in `schedules-client-component.tsx` (the other client component), which already has a separate `allSchedules` state for global stats — that's fine.

For `schedules-client-component-api.tsx`, there are no stats cards rendered (confirmed by reading the file). The bug description says "Stats cards always show 22 active, 0 inactive regardless of filters" — this is in `schedules-client-component.tsx` which computes stats from `allSchedules` (fetched without filters via `loadStats`). The fix: change the stats computation (lines 181-189) to derive from `schedules` (the filtered result) instead of `allSchedules` when filters are active:

```typescript
const statsSource = (statusFilter !== 'all' || resourceFilter !== 'all' || searchTerm) ? schedules : allSchedules;
const stats = {
    total: statsSource.length,
    active: statsSource.filter((s) => s.active).length,
    inactive: statsSource.filter((s) => !s.active).length,
    totalSavings: statsSource.reduce((sum, s) => sum + (s.estimatedSavings || 0), 0),
};
```

Also update `web-ui/components/schedules/schedules-client-component.tsx` to add this file to the task.

**BUG-7 — Fix E2E status filter test in schedules-pg.spec.ts:**
The test at line 92 selects "Active Only" from dropdown but doesn't account for the fact that `schedules-client-component-api.tsx` uses `useEffect` on `statusFilter` change which sends `?active=true` (not `?status=active`). The test waits for `?status` in the URL but the actual param is `?active`.

Fix the test:
1. Set up the request interception BEFORE clicking the dropdown (move `waitForRequest` promise creation before the click).
2. Change the URL check from `req.url().includes('status')` to `req.url().includes('/api/schedules')` and verify the request fires after selection.
3. The `schedules-client-component-api.tsx` sends `active=true` param, so check for that: `url.searchParams.get('active') === 'true'`.

Alternatively, if the page uses `schedules-client-component.tsx` (which uses `ClientScheduleService` that sends `?status=active`), match accordingly. Check which page component is actually rendered at `/app/schedules` — it's `page.tsx` which renders `SchedulesPageClient`. Verify what params that sends and match the test to reality.
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration && npx playwright test tests/e2e/schedules-pg.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>Zero duplicate key warnings in all 4 components, stats cards update when filters active, E2E status filter test passes without timeout</done>
</task>

</tasks>

<verification>
1. `cd web-ui && npx vitest run` — all existing unit tests pass
2. `npx playwright test tests/e2e/schedules-pg.spec.ts` — E2E tests pass including status filter test
3. Open browser console on /app/schedules — zero "duplicate key" warnings
4. API response `GET /api/schedules` returns deduplicated resourceTypes
5. Days display in Mon-Sun order in grid, table, and list views
6. Active schedules show computed nextExecution dates
7. `POST /api/schedules` with starttime===endtime returns 400
</verification>

<success_criteria>
- All 7 bugs resolved
- No regressions in existing unit tests
- E2E schedule tests pass
- Zero React key warnings in console
</success_criteria>

<output>
After completion, create `.planning/quick/260331-00a-fix-7-schedule-bugs-duplicate-react-keys/260331-00a-SUMMARY.md`
</output>
