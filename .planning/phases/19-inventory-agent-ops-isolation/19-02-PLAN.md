---
phase: 19-inventory-agent-ops-isolation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/lib/db/repositories/agent-ops-run/postgres.ts
  - web-ui/lib/db/repositories/agent-ops-event/postgres.ts
  - web-ui/lib/db/repositories/scheduled-task/postgres.ts
  - web-ui/lib/agent-ops/agent-ops-service.ts
  - web-ui/app/api/agent-ops/route.ts
  - web-ui/app/api/agent-ops/[runId]/route.ts
  - web-ui/app/api/agent-ops/[runId]/approve/route.ts
  - web-ui/app/api/agent-ops/[runId]/cancel/route.ts
  - web-ui/app/api/agent-ops/[runId]/resume/route.ts
  - web-ui/app/api/agent-ops/scheduled-tasks/route.ts
  - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/route.ts
  - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/pause/route.ts
  - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/resume/route.ts
  - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/trigger/route.ts
  - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/runs/route.ts
autonomous: true
requirements: [AIOP-01, AIOP-02, AIOP-03, AIOP-04]

must_haves:
  truths:
    - "Agent ops run list shows only the active tenant's runs"
    - "Agent ops run detail for another tenant's run returns 404"
    - "Agent ops run sub-routes (approve, cancel, resume) return 403 for cross-tenant runs"
    - "Agent ops events for a run are scoped to the tenant"
    - "Scheduled task list shows only the active tenant's tasks"
    - "Scheduled task mutations (pause, resume, trigger, delete) return 403 for cross-tenant tasks"
    - "AuditService calls in agent-ops routes include tenantId per D-10"
  artifacts:
    - path: "web-ui/lib/db/repositories/agent-ops-run/postgres.ts"
      provides: "Tenant-scoped agent ops run repository"
      contains: "getTenantClient"
    - path: "web-ui/lib/db/repositories/agent-ops-event/postgres.ts"
      provides: "Tenant-scoped agent ops event repository"
      contains: "getTenantClient"
    - path: "web-ui/lib/db/repositories/scheduled-task/postgres.ts"
      provides: "Tenant-scoped scheduled task repository"
      contains: "getTenantClient"
    - path: "web-ui/app/api/agent-ops/route.ts"
      provides: "Tenant-scoped run list API"
      contains: "getSessionTenantId"
    - path: "web-ui/app/api/agent-ops/[runId]/route.ts"
      provides: "Tenant-scoped run detail with pre-flight check"
      contains: "getSessionTenantId"
  key_links:
    - from: "web-ui/app/api/agent-ops/[runId]/approve/route.ts"
      to: "web-ui/lib/agent-ops/agent-ops-service.ts"
      via: "getSessionTenantId() -> agentOpsService.getRun(tenantId, runId) pre-flight"
      pattern: "getSessionTenantId"
    - from: "web-ui/lib/db/repositories/agent-ops-run/postgres.ts"
      to: "web-ui/lib/db/pg-config.ts"
      via: "getTenantClient(tenantId) replaces getPrismaClient()"
      pattern: "getTenantClient"
    - from: "web-ui/lib/db/repositories/scheduled-task/postgres.ts"
      to: "web-ui/lib/db/pg-config.ts"
      via: "getTenantClient(tenantId) replaces getPrismaClient()"
      pattern: "getTenantClient"
---

<objective>
Harden Agent Ops and Scheduled Tasks modules with tenant isolation — migrate 3 repositories from getPrismaClient() to getTenantClient(tenantId), fix all agent ops API routes to derive tenantId from session, add pre-flight 403 ownership checks on mutation sub-routes, and ensure AuditService calls include tenantId (D-10).

Purpose: Prevent cross-tenant agent ops data leakage and unauthorized mutations (AIOP-01, AIOP-02, AIOP-03, AIOP-04).
Output: 3 tenant-scoped repositories + 11 hardened API routes with pre-flight checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-inventory-agent-ops-isolation/19-CONTEXT.md

<interfaces>
<!-- Reference: AccountPostgresRepository (Phase 18 tenant client pattern) -->
From web-ui/lib/db/repositories/account/postgres.ts:
```typescript
import { getTenantClient } from '@/lib/db/pg-config';
// Every method: getTenantClient(tenantId).model.operation(...)
```

<!-- Reference: Pre-flight 403 pattern from Phase 18 -->
From web-ui/app/api/accounts/[accountId]/route.ts:
```typescript
// Pre-flight ownership check (D-03)
const existing = await AccountService.getAccount(accountId, tenantId);
if (!existing) {
    return NextResponse.json({ success: false, error: 'Forbidden' }, { status: 403 });
}
```

From web-ui/lib/auth-session.ts:
```typescript
export async function getSessionTenantId(): Promise<string>; // throws if no session/tenant
```

From web-ui/lib/agent-ops/agent-ops-service.ts:
```typescript
export async function getRun(tenantId: string, runId: string): Promise<AgentOpsRun | null>;
export async function listRuns(query: RunListQuery): Promise<{ runs: AgentOpsRun[]; lastKey?: Record<string, unknown> }>;
export async function getRunEvents(runId: string): Promise<AgentOpsEvent[]>;
// NOTE: getRunEvents currently hardcodes tenantId='default' — must be fixed
```

From web-ui/lib/db/repositories/agent-ops-event/interface.ts:
```typescript
export interface IAgentOpsEventRepository {
    recordEvent(params: RecordEventParams): Promise<void>;
    getRunEvents(runId: string, tenantId: string): Promise<AgentOpsEvent[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 3 Agent Ops repositories to getTenantClient</name>
  <files>
    web-ui/lib/db/repositories/agent-ops-run/postgres.ts,
    web-ui/lib/db/repositories/agent-ops-event/postgres.ts,
    web-ui/lib/db/repositories/scheduled-task/postgres.ts,
    web-ui/lib/agent-ops/agent-ops-service.ts
  </files>
  <read_first>
    - web-ui/lib/db/repositories/agent-ops-run/postgres.ts (13 occurrences of getPrismaClient())
    - web-ui/lib/db/repositories/agent-ops-event/postgres.ts (2 occurrences of getPrismaClient())
    - web-ui/lib/db/repositories/scheduled-task/postgres.ts (12 occurrences of getPrismaClient())
    - web-ui/lib/db/repositories/account/postgres.ts (reference: getTenantClient pattern)
    - web-ui/lib/db/pg-config.ts (getTenantClient signature, TENANT_SCOPED_MODELS)
    - web-ui/lib/agent-ops/agent-ops-service.ts (getRunEvents hardcodes 'default')
  </read_first>
  <action>
Per D-01 and D-02: Replace every `getPrismaClient()` with `getTenantClient(tenantId)` in all three repositories.

**AgentOpsRunPostgresRepository (agent-ops-run/postgres.ts):**
- Change import: `import { getTenantClient } from '@/lib/db/pg-config'` (remove getPrismaClient).
- `createRun(params)`: tenantId is `params.tenantId` -> use `getTenantClient(params.tenantId)`.
- `updateRunStatus(tenantId, runId, ...)`: use `getTenantClient(tenantId)`.
- `updateRunTrigger(tenantId, runId, ...)`: use `getTenantClient(tenantId)`.
- `updateApprovalMessageTs(tenantId, runId, ...)`: use `getTenantClient(tenantId)`.
- `getRun(tenantId, runId)`: use `getTenantClient(tenantId)`.
- `listRuns(query)`: tenantId is `query.tenantId`. IMPORTANT: the current code has a bypass `if (query.tenantId !== 'default' && query.tenantId !== 'all')` — remove the 'default' and 'all' bypass. Always scope by tenantId. If `query.tenantId` is falsy, throw an error. Use `getTenantClient(query.tenantId!)` and always set `where.tenantId = query.tenantId`.
- `listRunsBySource(source, limit)`: This method has NO tenantId parameter — it lists across all tenants. This is used by webhook handlers (Slack/Jira) that operate cross-tenant. Keep using `getPrismaClient()` for this one method only (add back a selective import of getPrismaClient). Actually — re-read the method. It's only called from `agent-ops-service.ts listRunsBySource`. Check callers. If only used by webhook handlers, keep unscoped. Add a comment: `// Cross-tenant: webhook handlers need to find runs across tenants`.
- `findAwaitingApprovalRun(runId)`: Cross-tenant lookup by design (AOPS-06 — Slack/Jira webhooks). Keep `getPrismaClient()`. Add comment.
- `findAwaitingApprovalRunByJiraIssue(issueKey)`: Cross-tenant. Keep `getPrismaClient()`. Add comment.
- `findAwaitingRunByJiraIssue(issueKey)`: Cross-tenant. Keep `getPrismaClient()`. Add comment.
- `findAwaitingRunBySlackThread(channelId, threadTs)`: Cross-tenant. Keep `getPrismaClient()`. Add comment.

So import BOTH: `import { getPrismaClient, getTenantClient } from '@/lib/db/pg-config'`. Use getTenantClient for all tenant-scoped methods, getPrismaClient only for the 4 cross-tenant webhook lookup methods.

**AgentOpsEventPostgresRepository (agent-ops-event/postgres.ts):**
- Change import: `import { getTenantClient, getPrismaClient } from '@/lib/db/pg-config'`.
- `recordEvent(params)`: Currently hardcodes `tenantId: 'org-default'`. The `RecordEventParams` interface likely doesn't include tenantId. Check the interface. If it doesn't have tenantId, the event's tenantId should be resolved from the run. For now, the recordEvent is called from agent-ops-service which doesn't pass tenantId. Two options: (a) add tenantId to RecordEventParams, or (b) look up the run's tenantId. Option (a) is simpler. Add `tenantId?: string` to RecordEventParams in the interface file. In the create call, use `params.tenantId || 'org-default'` as fallback. Use `getTenantClient(params.tenantId || 'org-default')` — but if tenantId is unknown, fall back to `getPrismaClient()` to avoid the middleware throwing on empty tenantId.
  Actually, simpler approach: keep `getPrismaClient()` for `recordEvent` since it explicitly sets tenantId in the data field and must never fail (errors are swallowed). The tenant middleware would inject tenantId anyway, but the explicit data field is fine. The key fix is `getRunEvents`.
- `getRunEvents(runId, tenantId)`: Already receives tenantId. Replace `getPrismaClient()` with `getTenantClient(tenantId)`.

**ScheduledTaskPostgresRepository (scheduled-task/postgres.ts):**
- Change import: `import { getPrismaClient, getTenantClient } from '@/lib/db/pg-config'`.
- All methods that receive `tenantId` parameter: `createScheduledTask`, `getScheduledTask`, `listScheduledTasks`, `updateScheduledTask`, `pauseScheduledTask`, `resumeScheduledTask`, `deleteScheduledTask`, `updateLastRun` -> use `getTenantClient(tenantId)`.
- `listAllActiveTasks()`: No tenantId — used by the scheduler engine to find all active tasks across tenants. Keep `getPrismaClient()`. Add comment: `// Cross-tenant: scheduler engine scans all active tasks`.
- `tryAcquireExecutionLock(taskId, scheduledAt)`: Uses `$executeRaw` and `scheduledTaskLock` (not in TENANT_SCOPED_MODELS). Keep `getPrismaClient()`. Add comment: `// Platform-level: locks are not tenant-scoped`.

**agent-ops-service.ts:**
- Fix `getRunEvents(runId)` on line 131-133: currently hardcodes `'default'` as tenantId. Change signature to `getRunEvents(runId: string, tenantId: string)` and pass through. Update the `agentOpsService` object export to match.
- Update the `agentOpsService` object at the bottom to include the updated signature.
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy/web-ui && echo "=== agent-ops-run ===" && grep -c "getTenantClient" lib/db/repositories/agent-ops-run/postgres.ts && echo "=== agent-ops-event ===" && grep -c "getTenantClient" lib/db/repositories/agent-ops-event/postgres.ts && echo "=== scheduled-task ===" && grep -c "getTenantClient" lib/db/repositories/scheduled-task/postgres.ts && echo "=== service default ===" && grep -c "'default'" lib/agent-ops/agent-ops-service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getTenantClient" web-ui/lib/db/repositories/agent-ops-run/postgres.ts` returns at least 7 matches (tenant-scoped methods)
    - `grep "getTenantClient" web-ui/lib/db/repositories/agent-ops-event/postgres.ts` returns at least 1 match (getRunEvents)
    - `grep "getTenantClient" web-ui/lib/db/repositories/scheduled-task/postgres.ts` returns at least 10 matches
    - `grep "'default'" web-ui/lib/agent-ops/agent-ops-service.ts` returns 0 matches (no hardcoded default in getRunEvents)
    - `grep "getRunEvents(runId: string, tenantId: string)" web-ui/lib/agent-ops/agent-ops-service.ts` returns 1 match
    - Cross-tenant methods (findAwaitingApprovalRun, listAllActiveTasks, tryAcquireExecutionLock) still use getPrismaClient
    - Files compile: `cd web-ui && npx tsc --noEmit --pretty 2>&1 | grep -c "error TS"` returns 0
  </acceptance_criteria>
  <done>All 3 agent ops repositories use getTenantClient for tenant-scoped operations. Cross-tenant webhook/scheduler methods explicitly documented and kept on getPrismaClient. getRunEvents in service layer accepts tenantId parameter.</done>
</task>

<task type="auto">
  <name>Task 2: Harden agent ops API routes with session tenantId, pre-flight 403 checks, and AuditService tenantId (D-10)</name>
  <files>
    web-ui/app/api/agent-ops/route.ts,
    web-ui/app/api/agent-ops/[runId]/route.ts,
    web-ui/app/api/agent-ops/[runId]/approve/route.ts,
    web-ui/app/api/agent-ops/[runId]/cancel/route.ts,
    web-ui/app/api/agent-ops/[runId]/resume/route.ts,
    web-ui/app/api/agent-ops/scheduled-tasks/route.ts,
    web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/route.ts,
    web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/pause/route.ts,
    web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/resume/route.ts,
    web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/trigger/route.ts,
    web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/runs/route.ts
  </files>
  <read_first>
    - web-ui/app/api/agent-ops/route.ts (tenantId from query param, not session)
    - web-ui/app/api/agent-ops/[runId]/route.ts (tenantId from query param)
    - web-ui/app/api/agent-ops/[runId]/approve/route.ts (tenantId from body)
    - web-ui/app/api/agent-ops/[runId]/cancel/route.ts (tenantId from body)
    - web-ui/app/api/agent-ops/[runId]/resume/route.ts (tenantId from body)
    - web-ui/app/api/agent-ops/scheduled-tasks/route.ts (tenantId from query/body)
    - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/route.ts (tenantId from query/body)
    - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/pause/route.ts (tenantId from body)
    - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/resume/route.ts (tenantId from body)
    - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/trigger/route.ts (tenantId from body)
    - web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/runs/route.ts (tenantId from query)
    - web-ui/app/api/accounts/[accountId]/route.ts (reference: pre-flight 403 pattern)
    - web-ui/lib/auth-session.ts (getSessionTenantId)
    - web-ui/lib/audit-service.ts (AuditService.logUserAction / logResourceAction signatures — check tenantId param shape)
  </read_first>
  <action>
Per D-05, D-06, D-07, D-08: All routes derive tenantId from session. Mutation sub-routes add pre-flight ownership checks returning 403.

**Pattern for every route:** Add `import { getSessionTenantId } from '@/lib/auth-session';` and replace `tenantId` derivation from query params/body with `const tenantId = await getSessionTenantId();`. Remove any `|| 'default'` fallbacks.

**agent-ops/route.ts (AIOP-01 — list runs):**
- Replace `const tenantId = url.searchParams.get('tenantId') || undefined;` with `const tenantId = await getSessionTenantId();`
- Pass tenantId to listRuns query (already does, just fix the source).

**agent-ops/[runId]/route.ts (AIOP-01, AIOP-03 — run detail + events):**
- Replace `const tenantId = url.searchParams.get('tenantId') || 'default';` with `const tenantId = await getSessionTenantId();`
- Fix `agentOpsService.getRunEvents(runId)` call — now needs tenantId: `agentOpsService.getRunEvents(runId, tenantId)`.
- The existing `if (!run)` -> 404 already serves as the pre-flight check for GET (per D-06, reads return 404 not 403).

**agent-ops/[runId]/approve/route.ts (D-06 pre-flight):**
- Replace `const tenantId: string = body.tenantId || 'default';` with `const tenantId = await getSessionTenantId();`
- The existing `agentOpsService.getRun(tenantId, runId)` + `if (!run)` -> 404 already serves as pre-flight. Change the 404 to 403: `return NextResponse.json({ success: false, error: 'Forbidden' }, { status: 403 });` per D-06.

**agent-ops/[runId]/cancel/route.ts (D-06 pre-flight):**
- Replace `const tenantId = body.tenantId || 'default';` with `const tenantId = await getSessionTenantId();`
- Change the existing `if (!run)` -> 404 to 403: `return NextResponse.json({ success: false, error: 'Forbidden' }, { status: 403 });`

**agent-ops/[runId]/resume/route.ts (D-06 pre-flight):**
- Replace `const { userInput, tenantId = 'default' } = body;` with:
  ```
  const tenantId = await getSessionTenantId();
  const { userInput } = body;
  ```
- Change the existing `if (!run)` -> 404 to 403: `return NextResponse.json({ success: false, error: 'Forbidden' }, { status: 403 });`

**scheduled-tasks/route.ts (AIOP-04 — list + create):**
- GET: Replace `const tenantId = new URL(req.url).searchParams.get('tenantId') || 'default';` with `const tenantId = await getSessionTenantId();`
- POST: Replace `tenantId: body.tenantId || 'default'` with `const tenantId = await getSessionTenantId();` and use that in the createScheduledTask params. Also set `createdBy` from session if possible (or keep body.createdBy).

**scheduled-tasks/[taskId]/route.ts (AIOP-04 — get, update, delete):**
- GET: Replace `const tenantId = new URL(req.url).searchParams.get('tenantId') || 'default';` with `const tenantId = await getSessionTenantId();`
- PATCH (D-08 pre-flight): Replace `const tenantId = body.tenantId || 'default';` with `const tenantId = await getSessionTenantId();`. Add pre-flight: before calling updateScheduledTask, call `getScheduledTask(tenantId, taskId)` — if null, return 403.
- DELETE (D-08 pre-flight): Replace `const tenantId = new URL(req.url).searchParams.get('tenantId') || 'default';` with `const tenantId = await getSessionTenantId();`. Add pre-flight: call `getScheduledTask(tenantId, taskId)` — if null, return 403.

**scheduled-tasks/[taskId]/pause/route.ts (D-08 pre-flight):**
- Replace `const { tenantId = 'default' } = await req.json().catch(() => ({}));` with `const tenantId = await getSessionTenantId();`
- The existing `getScheduledTask(tenantId, taskId)` + `if (!task)` -> 404 already serves as pre-flight. Change to 403.

**scheduled-tasks/[taskId]/resume/route.ts (D-08 pre-flight):**
- Replace `const { tenantId = 'default' } = await req.json().catch(() => ({}));` with `const tenantId = await getSessionTenantId();`
- The existing `resumeScheduledTask` returns null if not found. Add explicit pre-flight: call `getScheduledTask(tenantId, taskId)` first, return 403 if null. Then call resumeScheduledTask. Import `getScheduledTask` from scheduled-task-service.

**scheduled-tasks/[taskId]/trigger/route.ts (D-08 pre-flight):**
- Replace `const { tenantId = 'default' } = await req.json().catch(() => ({}));` with `const tenantId = await getSessionTenantId();`
- The existing `getScheduledTask(tenantId, taskId)` + `if (!task)` -> 404 already serves as pre-flight. Change to 403.

**scheduled-tasks/[taskId]/runs/route.ts (D-08 — read, no 403):**
- Replace `const tenantId = url.searchParams.get('tenantId') || 'default';` with `const tenantId = await getSessionTenantId();`
- Per D-08: this is a read route — scope by tenantId + taskId, no separate 403 check needed.

**D-10 — AuditService tenantId in agent-ops routes:**
- For any `AuditService.logUserAction` or `AuditService.logResourceAction` call in the modified agent-ops routes, add `tenantId` to the params object (sourced from `getSessionTenantId()` already called at route level).
- Check the AuditService signature: if it accepts `tenantId` as a top-level param, pass it there. If it only accepts it in `metadata`, add it to metadata.
- Verify: `grep -rn "AuditService" web-ui/app/api/agent-ops/ | grep -v "tenantId"` should return 0 matches after this task (every AuditService call includes tenantId).
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy/web-ui && echo "=== session check ===" && grep -rl "getSessionTenantId" app/api/agent-ops/ | wc -l | tr -d ' ' && echo "=== no default fallback ===" && grep -rl "'default'" app/api/agent-ops/ | wc -l | tr -d ' ' && echo "=== D-10 audit tenantId ===" && grep -rn "AuditService" app/api/agent-ops/ | grep -v "tenantId" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "getSessionTenantId" web-ui/app/api/agent-ops/` returns 11 files (all agent-ops routes)
    - `grep -rl "'default'" web-ui/app/api/agent-ops/` returns 0 files (no hardcoded defaults)
    - `grep "status: 403" web-ui/app/api/agent-ops/[runId]/approve/route.ts` returns at least 1 match
    - `grep "status: 403" web-ui/app/api/agent-ops/[runId]/cancel/route.ts` returns at least 1 match
    - `grep "status: 403" web-ui/app/api/agent-ops/[runId]/resume/route.ts` returns at least 1 match
    - `grep "status: 403" web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/pause/route.ts` returns at least 1 match
    - `grep "status: 403" web-ui/app/api/agent-ops/scheduled-tasks/[taskId]/trigger/route.ts` returns at least 1 match
    - `grep "getRunEvents(runId, tenantId)" web-ui/app/api/agent-ops/[runId]/route.ts` returns 1 match
    - `grep -rn "AuditService" web-ui/app/api/agent-ops/ | grep -v "tenantId"` returns 0 matches (D-10: every AuditService call includes tenantId)
    - All files compile: `cd web-ui && npx tsc --noEmit --pretty 2>&1 | grep "agent-ops" | grep -c "error TS"` returns 0
  </acceptance_criteria>
  <done>All 11 agent ops API routes derive tenantId from getSessionTenantId(). Run sub-routes (approve, cancel, resume) return 403 for cross-tenant access per D-06. Scheduled task mutations (pause, resume, trigger, delete, update) return 403 per D-08. AuditService calls include tenantId per D-10. No hardcoded 'default' tenantId values remain.</done>
</task>

</tasks>

<verification>
1. `grep -r "getPrismaClient" web-ui/lib/db/repositories/agent-ops-run/postgres.ts` returns matches ONLY for cross-tenant webhook methods (findAwaiting*)
2. `grep -r "getPrismaClient" web-ui/lib/db/repositories/scheduled-task/postgres.ts` returns matches ONLY for listAllActiveTasks and tryAcquireExecutionLock
3. `grep -rl "'default'" web-ui/app/api/agent-ops/` returns 0 files
4. All 11 agent-ops routes import getSessionTenantId
5. Pre-flight 403 checks present on approve, cancel, resume, pause, trigger, delete, update routes
6. `grep -rn "AuditService" web-ui/app/api/agent-ops/ | grep -v "tenantId"` returns 0 matches (D-10)
7. `cd web-ui && npx tsc --noEmit` completes without errors
</verification>

<success_criteria>
- 3 repositories use getTenantClient for all tenant-scoped operations
- Cross-tenant methods (webhook lookups, scheduler scan, lock acquisition) explicitly documented and kept on getPrismaClient
- All 11 agent ops API routes derive tenantId from authenticated session
- Mutation sub-routes return 403 for cross-tenant access attempts
- getRunEvents in service layer accepts and passes tenantId
- AuditService calls in agent-ops routes include tenantId (D-10)
- No hardcoded 'default' tenantId values in any agent ops route
</success_criteria>

<output>
After completion, create `.planning/phases/19-inventory-agent-ops-isolation/19-02-SUMMARY.md`
</output>
