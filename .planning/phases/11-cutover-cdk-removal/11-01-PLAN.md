---
phase: 11-cutover-cdk-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-env.ts
autonomous: true
requirements:
  - PULUMI-16

must_haves:
  truths:
    - "Running `npx tsx scripts/generate-env.ts` produces a valid web-ui/.env.local"
    - "All Pulumi stack outputs are mapped to the correct env var names"
    - "Cognito client secret is resolved (not [secret]) via --show-secrets flag"
    - "Static values (AWS_REGION, NODE_ENV, BEDROCK_MODEL_ID, etc.) are included"
  artifacts:
    - path: "scripts/generate-env.ts"
      provides: "Pulumi stack output to .env.local generator"
      min_lines: 80
  key_links:
    - from: "scripts/generate-env.ts"
      to: "infra/compute/index.ts"
      via: "pulumi stack output --json --show-secrets"
      pattern: "pulumi stack output"
    - from: "scripts/generate-env.ts"
      to: "web-ui/.env.local"
      via: "fs.writeFileSync"
      pattern: "writeFileSync.*\\.env\\.local"
---

<objective>
Create `scripts/generate-env.ts` that reads Pulumi compute stack outputs and writes `web-ui/.env.local` with all required environment variables.

Purpose: Automates local dev environment setup — no manual copy-paste of 30+ env vars from Pulumi outputs.
Output: `scripts/generate-env.ts` script that produces a working `.env.local` file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cutover-cdk-removal/11-CONTEXT.md

@infra/compute/index.ts (stack outputs — lines 1610-1824)

<interfaces>
<!-- Pulumi compute stack exports (from infra/compute/index.ts) that generate-env.ts must map -->

Stack output keys (camelCase export names become the JSON keys in `pulumi stack output --json`):
```
appTableName, auditTableName, inventoryTableName, usersTeamsTableName,
checkpointTableName, writesTableName, chatHistoryTableName, memoryTableName,
agentOpsTableName, checkpointBucketName, agentTempBucketName, kbStagingBucketName,
inventoryBucketName, cognitoUserPoolId, cognitoUserPoolClientId,
cognitoUserPoolClientSecret (secret), cognitoIdentityPoolId, cognitoDomainPrefix,
schedulerLambdaArn, cloudFrontUrl, albDnsName, kbSyncQueueUrl,
vectorProcessingQueueUrl, ecsClusterArn, ecrRepositoryUri,
originVerifySecretValue (secret)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate-env.ts script</name>
  <files>scripts/generate-env.ts</files>
  <read_first>
    - infra/compute/index.ts (lines 1610-1824 — all export const lines for output key names)
    - .planning/phases/11-cutover-cdk-removal/11-CONTEXT.md (locked env var mappings)
  </read_first>
  <action>
Create `scripts/generate-env.ts` that:

1. Runs `pulumi stack output --json --show-secrets --stack prod --cwd infra/compute/` via `child_process.execSync` (per D: generate-env.ts LOCKED decision)
2. Parses the JSON output into a Record of string keys
3. Maps each Pulumi output key to one or more env var names using the LOCKED mapping from CONTEXT.md:

```
appTableName -> APP_TABLE_NAME, NEXT_PUBLIC_APP_TABLE_NAME
auditTableName -> AUDIT_TABLE_NAME, NEXT_PUBLIC_AUDIT_TABLE_NAME
checkpointTableName -> DYNAMODB_CHECKPOINT_TABLE
writesTableName -> DYNAMODB_WRITES_TABLE
checkpointBucketName -> CHECKPOINT_S3_BUCKET
chatHistoryTableName -> DYNAMODB_CHAT_HISTORY_TABLE
memoryTableName -> DYNAMODB_MEMORY_TABLE
usersTeamsTableName -> DYNAMODB_USERS_TEAMS_TABLE
cognitoUserPoolId -> COGNITO_USER_POOL_ID, NEXT_PUBLIC_COGNITO_USER_POOL_ID
cognitoUserPoolClientId -> COGNITO_USER_POOL_CLIENT_ID, NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID
cognitoUserPoolClientSecret -> COGNITO_CLIENT_SECRET, COGNITO_APP_CLIENT_SECRET
cognitoIdentityPoolId -> COGNITO_IDENTITY_POOL_ID, NEXT_PUBLIC_COGNITO_IDENTITY_POOL_ID
cognitoDomainPrefix -> construct COGNITO_DOMAIN and NEXT_PUBLIC_COGNITO_DOMAIN as `${prefix}.auth.us-east-1.amazoncognito.com`
cloudFrontUrl -> NEXTAUTH_URL, NEXT_PUBLIC_NEXTAUTH_URL
schedulerLambdaArn -> SCHEDULER_LAMBDA_ARN
inventoryBucketName -> INVENTORY_BUCKET_NAME
kbSyncQueueUrl -> KB_SYNC_QUEUE_URL
kbStagingBucketName -> KB_STAGING_BUCKET_NAME
agentTempBucketName -> AGENT_TEMP_BUCKET
agentOpsTableName -> AGENT_OPS_TABLE_NAME
inventoryTableName -> INVENTORY_TABLE_NAME
```

4. Appends static values (not from stack outputs):
```
AWS_REGION=us-east-1
NEXT_PUBLIC_AWS_REGION=us-east-1
NODE_ENV=production
PORT=3000
BEDROCK_MODEL_ID=amazon.titan-embed-text-v2:0
VECTOR_INDEX_NAME=text-embeddings
KB_VECTOR_INDEX_NAME=knowledge-base-embeddings
ASK_AI_GENERATION_MODEL=global.anthropic.claude-sonnet-4-6
LANGFUSE_ENABLED=false
DATA_DIR=/tmp
AWS_USE_STS=true
NEXT_PUBLIC_AWS_USE_STS=true
```

5. Writes the combined output to `web-ui/.env.local` using `fs.writeFileSync`
6. Prints a summary: number of vars written, output path

Error handling:
- If `pulumi` CLI not found, print helpful message and exit 1
- If stack output JSON is empty or missing expected keys, warn but continue (some outputs may be optional)
- Use `#!/usr/bin/env npx tsx` shebang so it can run as `npx tsx scripts/generate-env.ts`

Runtime: `npx tsx scripts/generate-env.ts` (tsx already in devDependencies)
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/pulumi-migration && npx tsc --noEmit --esModuleInterop --module commonjs --moduleResolution node --target ES2020 --strict scripts/generate-env.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - `scripts/generate-env.ts` exists with all mappings from CONTEXT.md
    - Script uses `--show-secrets` flag in the pulumi command
    - Script writes to `web-ui/.env.local` (not root .env)
    - All 20+ dynamic mappings and 12 static values are present
    - TypeScript compiles without errors
  </done>
  <acceptance_criteria>
    - grep "show-secrets" scripts/generate-env.ts returns a match
    - grep "web-ui/.env.local" scripts/generate-env.ts returns a match
    - grep "APP_TABLE_NAME" scripts/generate-env.ts returns a match
    - grep "COGNITO_CLIENT_SECRET" scripts/generate-env.ts returns a match
    - grep "BEDROCK_MODEL_ID" scripts/generate-env.ts returns a match
    - grep "writeFileSync" scripts/generate-env.ts returns a match
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `scripts/generate-env.ts` compiles with tsc --noEmit
- All CONTEXT.md mappings present (grep for each key env var name)
- `--show-secrets` flag used in pulumi command
- Output path is `web-ui/.env.local`
</verification>

<success_criteria>
- Script exists and compiles
- All 20+ dynamic output mappings from CONTEXT.md are implemented
- All 12 static values are included
- Cognito domain is constructed from prefix (not a direct mapping)
- Running the script with Pulumi available would produce a valid .env.local
</success_criteria>

<output>
After completion, create `.planning/phases/11-cutover-cdk-removal/11-01-SUMMARY.md`
</output>
