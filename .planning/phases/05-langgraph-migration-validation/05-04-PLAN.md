---
phase: 05-langgraph-migration-validation
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/e2e/agent-chat.spec.ts
autonomous: false
requirements: [LANG-07]
must_haves:
  truths:
    - "Agent chat sends a message and receives a streamed response"
    - "Thread history persists across page refresh"
    - "Chat messages appear in correct chronological order"
  artifacts:
    - path: "tests/e2e/agent-chat.spec.ts"
      provides: "Playwright E2E tests for agent chat with PostgreSQL persistence"
      min_lines: 40
  key_links:
    - from: "tests/e2e/agent-chat.spec.ts"
      to: "/api/chat"
      via: "POST request through chat UI"
      pattern: "chat"
    - from: "tests/e2e/agent-chat.spec.ts"
      to: "/api/threads"
      via: "Thread history API"
      pattern: "threads"
---

<objective>
Write Playwright E2E tests verifying agent chat, thread history persistence, and memory recall work end-to-end on PostgreSQL. Human checkpoint to verify visual correctness.

Purpose: Final validation that the LangGraph persistence migration works from the user's perspective — chat works, threads persist, history loads.
Output: E2E test file, human verification of agent chat flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-langgraph-migration-validation/05-CONTEXT.md
@.planning/phases/05-langgraph-migration-validation/05-02-SUMMARY.md

@tests/e2e/accounts.spec.ts
@web-ui/app/api/chat/route.ts
@web-ui/app/api/threads/[threadId]/history/route.ts
@web-ui/components/agent/chat-interface.tsx
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright E2E tests for agent chat and thread history</name>
  <files>tests/e2e/agent-chat.spec.ts</files>
  <read_first>
    - tests/e2e/accounts.spec.ts (existing E2E test pattern — imports, describe structure, locator style, beforeEach)
    - playwright.config.ts (base URL, webServer config, project setup)
    - web-ui/components/agent/chat-interface.tsx (UI element names, data-testid attributes, input/button selectors)
    - web-ui/app/api/chat/route.ts (POST endpoint — understand request/response shape)
    - web-ui/app/api/threads/[threadId]/history/route.ts (GET endpoint — thread history shape)
  </read_first>
  <action>
    Create `tests/e2e/agent-chat.spec.ts` with Playwright tests for LANG-07:

    1. Test: "agent chat page loads with input field"
       - Navigate to the agent chat page (find the correct route from app/ directory)
       - Verify chat input field is visible (use getByRole or getByTestId)
       - Verify send button is visible

    2. Test: "user can send a message and receive a response"
       - Type a simple message in the chat input
       - Click send button
       - Wait for AI response region to become visible (NOT waitForTimeout)
       - Verify the user message appears in the chat
       - Verify an AI response appears (any content — don't assert exact text)

    3. Test: "thread history persists across page refresh"
       - Send a message and wait for response
       - Note the current URL (should contain threadId)
       - Reload the page
       - Wait for chat messages to load
       - Verify the previously sent message still appears
       - Verify the AI response still appears

    4. Test: "thread list shows previous conversations"
       - Navigate to threads/history page (if it exists as a UI route)
       - OR: verify the sidebar/thread list shows at least one thread
       - Verify clicking a thread loads its messages

    Follow existing E2E conventions:
    - Use `page.getByRole()`, `page.getByTestId()`, `page.getByText()` — never CSS selectors
    - Use `await expect(...).toBeVisible()` for waits — never `waitForTimeout`
    - Use `test.describe()` for grouping
    - Use `test.beforeEach()` for shared navigation

    Note: The exact selectors depend on the chat UI component structure. The executor MUST read chat-interface.tsx to find the correct element names/roles/testids. If data-testid attributes are missing, add them to the component.
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration && grep -q "test.describe" tests/e2e/agent-chat.spec.ts && grep -q "thread" tests/e2e/agent-chat.spec.ts && grep -q "chat" tests/e2e/agent-chat.spec.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "test.describe" tests/e2e/agent-chat.spec.ts returns a match
    - grep "getByRole\|getByTestId\|getByText" tests/e2e/agent-chat.spec.ts returns a match (proper locators)
    - grep "toBeVisible" tests/e2e/agent-chat.spec.ts returns a match (proper waits)
    - grep -v "waitForTimeout" tests/e2e/agent-chat.spec.ts confirms no timeout waits
    - grep "thread" tests/e2e/agent-chat.spec.ts returns a match (thread history test)
    - grep "refresh\|reload" tests/e2e/agent-chat.spec.ts returns a match (persistence test)
    - File has at least 3 test() blocks
  </acceptance_criteria>
  <done>Playwright E2E tests cover agent chat send/receive, thread history persistence across refresh, and thread listing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify agent chat works end-to-end on PostgreSQL</name>
  <what-built>
    Complete LangGraph persistence migration: persistence.ts rewritten with USE_PG_LANGGRAPH feature flag, pgvector Docker image, AgentMemory + ChatMessage Prisma models, checkpoint-postgres library, migrate-all.ts orchestration, verify-migration.ts validation, AgentConversationsTable CDK removal, and E2E tests.
  </what-built>
  <how-to-verify>
    1. Ensure Docker is running: `docker compose up -d`
    2. Apply migrations: `cd web-ui && npx prisma migrate deploy`
    3. Set env: `USE_PG_LANGGRAPH=true` in web-ui/.env.local
    4. Start dev server: `cd web-ui && npm run dev`
    5. Open http://localhost:3000 and navigate to the agent chat
    6. Send a message — verify you get a streamed response
    7. Refresh the page — verify the conversation persists
    8. Open a new thread — verify the old thread appears in history
    9. Run E2E tests: `npx playwright test tests/e2e/agent-chat.spec.ts`
    10. Run verify-migration: `AWS_PROFILE=PLATFORM-ADMIN npx tsx scripts/verify-migration.ts`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/agent-chat.spec.ts` passes
- Agent chat works visually with USE_PG_LANGGRAPH=true
- Thread history persists across page refresh
</verification>

<success_criteria>
- E2E tests pass, human verifies agent chat works on PostgreSQL, thread persistence confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/05-langgraph-migration-validation/05-04-SUMMARY.md`
</output>
