---
phase: 21-audit-settings-regression-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/lib/db/repositories/audit-log/postgres.ts
  - web-ui/app/api/audit/route.ts
  - web-ui/app/api/scheduler/execute/route.ts
  - web-ui/app/api/scheduler/settings/route.ts
  - web-ui/app/api/schedules/route.ts
  - web-ui/app/api/schedules/[scheduleId]/execute/route.ts
  - web-ui/app/api/inventory/sync/route.ts
autonomous: true
requirements: [AUDT-01, AUDT-02, STNG-04, STNG-05]

must_haves:
  truths:
    - "Audit log GET returns only the active tenant's entries"
    - "All audit log writes include tenantId in the persisted record"
    - "Tenant settings GET and PUT are scoped to the session tenant"
    - "Tenant logo POST and PUT are scoped to the session tenant"
  artifacts:
    - path: "web-ui/lib/db/repositories/audit-log/postgres.ts"
      provides: "Tenant-scoped audit log repository using getTenantClient"
      contains: "getTenantClient"
    - path: "web-ui/app/api/audit/route.ts"
      provides: "Audit route passing tenantId from session"
      contains: "getSessionTenantId"
  key_links:
    - from: "web-ui/app/api/audit/route.ts"
      to: "web-ui/lib/audit-service.ts"
      via: "AuditService.getAuditLogs(filters, tenantId)"
      pattern: "getAuditLogs.*tenantId"
    - from: "web-ui/lib/db/repositories/audit-log/postgres.ts"
      to: "web-ui/lib/db/pg-config.ts"
      via: "getTenantClient(tenantId)"
      pattern: "getTenantClient"
---

<objective>
Migrate AuditLogPostgresRepository to getTenantClient, fix the audit API route to scope by tenantId, sweep all AuditService call sites missing tenantId, and verify settings/logo routes are correctly scoped.

Purpose: Close AUDT-01 (read scoping), AUDT-02 (write scoping), STNG-04/STNG-05 (settings verification).
Output: Fully tenant-scoped audit log reads and writes; confirmed settings isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-audit-settings-regression-tests/21-CONTEXT.md

<interfaces>
<!-- Tenant client factory — the target pattern for audit repo migration -->
From web-ui/lib/db/pg-config.ts:
```typescript
export function getTenantClient(tenantId: string); // Returns Prisma client with tenant middleware
export function getPrismaClient(): PrismaClient;   // Raw client — being replaced
```

From web-ui/lib/audit-service.ts:
```typescript
export class AuditService {
  static async createAuditLog(auditData: Omit<AuditLog, 'id' | 'type' | 'timestamp'>): Promise<void>;
  static async getAuditLogs(filters?: AuditLogFilters, tenantId?: string): Promise<AuditLogResponse>;
  static async logUserAction(data: { action, resourceType, resourceId, resourceName, user, userType, status, details, metadata?, ... }): Promise<void>;
  static async logResourceAction(data: { action, resourceType, resourceId, resourceName, status, details, user?, metadata?, tenantId?, ... }): Promise<void>;
}
```

From web-ui/lib/auth-session.ts:
```typescript
export async function getSessionTenantId(): Promise<string>; // Throws if no session/tenant
export async function getAuthSession();
```

From web-ui/lib/db/repositories/audit-log/interface.ts:
```typescript
export interface IAuditLogRepository {
  createAuditLog(auditData: Omit<AuditLog, 'id' | 'type' | 'timestamp'>): Promise<void>;
  getAuditLogs(tenantId: string, filters?: AuditLogFilters): Promise<AuditLogResponse>;
}
```

<!-- Reference: already-migrated account repo pattern -->
From web-ui/lib/db/repositories/account/postgres.ts:
```typescript
import { getTenantClient } from '@/lib/db/pg-config';
// Every method calls getTenantClient(tenantId).account.findMany/create/update/deleteMany
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AuditLogPostgresRepository to getTenantClient and fix audit API route</name>
  <files>web-ui/lib/db/repositories/audit-log/postgres.ts, web-ui/app/api/audit/route.ts</files>
  <read_first>
    - web-ui/lib/db/repositories/audit-log/postgres.ts (current: uses getPrismaClient)
    - web-ui/lib/db/repositories/account/postgres.ts (reference: getTenantClient pattern)
    - web-ui/app/api/audit/route.ts (current: missing tenantId on GET)
    - web-ui/lib/audit-service.ts (getAuditLogs signature accepts tenantId as 2nd arg)
    - web-ui/lib/auth-session.ts (getSessionTenantId helper)
  </read_first>
  <action>
**Per D-01: Migrate AuditLogPostgresRepository from getPrismaClient() to getTenantClient(tenantId)**

1. In `web-ui/lib/db/repositories/audit-log/postgres.ts`:
   - Change import from `import { getPrismaClient } from '@/lib/db/pg-config'` to `import { getTenantClient } from '@/lib/db/pg-config'`
   - In `createAuditLog`: extract `tenantId` from `auditData` (same as current logic, line 43-44), then replace `getPrismaClient().auditLog.create(...)` with `getTenantClient(tenantId).auditLog.create(...)`. Keep the `'org-default'` fallback for backward compat.
   - In `getAuditLogs`: replace `getPrismaClient().auditLog.findMany(...)` with `getTenantClient(tenantId).auditLog.findMany(...)`. The `tenantId` parameter is already passed in as the first argument.

**Per D-02: Fix /api/audit/route.ts GET to pass tenantId**

2. In `web-ui/app/api/audit/route.ts`:
   - Add import: `import { getSessionTenantId } from '@/lib/auth-session'`
   - In the GET handler, after the authorize check, add: `const tenantId = await getSessionTenantId();`
   - Change `AuditService.getAuditLogs(filters)` to `AuditService.getAuditLogs(filters, tenantId)`
   - In the POST handler, add: `const tenantId = await getSessionTenantId();` and spread `tenantId` into the auditData passed to `AuditService.createAuditLog({ ...auditData, tenantId, userAgent, ipAddress, source: 'web-ui' })`
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy/web-ui && npx vitest run lib/db/repositories/audit-log/postgres.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getTenantClient" web-ui/lib/db/repositories/audit-log/postgres.ts (import changed)
    - grep -q "getSessionTenantId" web-ui/app/api/audit/route.ts (session tenant used)
    - grep -q "getAuditLogs(filters, tenantId)" web-ui/app/api/audit/route.ts (tenantId passed)
    - grep -v "getPrismaClient" web-ui/lib/db/repositories/audit-log/postgres.ts confirms no remaining getPrismaClient usage
  </acceptance_criteria>
  <done>AuditLogPostgresRepository uses getTenantClient(tenantId) for all queries. Audit GET route passes tenantId from session. Audit POST route includes tenantId in persisted data. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep all AuditService call sites missing tenantId and verify settings routes</name>
  <files>web-ui/app/api/scheduler/execute/route.ts, web-ui/app/api/scheduler/settings/route.ts, web-ui/app/api/schedules/route.ts, web-ui/app/api/schedules/[scheduleId]/execute/route.ts, web-ui/app/api/inventory/sync/route.ts</files>
  <read_first>
    - web-ui/app/api/scheduler/execute/route.ts (2 logUserAction calls, no tenantId)
    - web-ui/app/api/scheduler/settings/route.ts (2 logUserAction calls, no tenantId)
    - web-ui/app/api/schedules/route.ts (2 logUserAction calls — line 140 has metadata.tenantId but not in auditData root)
    - web-ui/app/api/schedules/[scheduleId]/execute/route.ts (1 logResourceAction, has metadata.tenantId but not root)
    - web-ui/app/api/inventory/sync/route.ts (2 logResourceAction calls — already has tenantId in spread, verify)
    - web-ui/app/api/tenants/settings/route.ts (verify: already uses getSessionTenantId)
    - web-ui/app/api/tenants/logo/route.ts (verify: already uses getSessionTenantId)
  </read_first>
  <action>
**Per D-03, D-04, D-05: Full sweep of AuditService call sites missing tenantId**

The `AuditService.logUserAction` and `logResourceAction` methods spread all data into `createAuditLog`. The `AuditLogPostgresRepository.createAuditLog` extracts `tenantId` from `auditData` (line 43-44 in current code). So `tenantId` must be a top-level property in the data object passed to `logUserAction`/`logResourceAction`.

Fix each call site:

1. **web-ui/app/api/scheduler/execute/route.ts** — 2 calls (lines 57, 79):
   - Add `import { getSessionTenantId } from '@/lib/auth-session'` (if not present)
   - Add `const tenantId = await getSessionTenantId();` near the top of the POST handler
   - Add `tenantId` property to both `logUserAction({...})` calls (the error one at line 57 and success one at line 79)

2. **web-ui/app/api/scheduler/settings/route.ts** — 2 calls (lines 126, 153):
   - Add `import { getSessionTenantId } from '@/lib/auth-session'`
   - In the PUT handler, add `const tenantId = await getSessionTenantId();`
   - Add `tenantId` property to both `logUserAction({...})` calls (success at line 126, error at line 153)

3. **web-ui/app/api/schedules/route.ts** — 2 calls (lines 140, 162):
   - `tenantId` is already available as a local variable (line 70). Move `tenantId` from `metadata: { tenantId }` to a top-level property: add `tenantId,` to both `logUserAction({...})` calls. Keep it in metadata too for backward compat.

4. **web-ui/app/api/schedules/[scheduleId]/execute/route.ts** — 1 call (line 127):
   - `tenantId` is already available as a local variable. Move `tenantId` from `metadata: { tenantId }` to a top-level property: add `tenantId,` to the `logResourceAction({...})` call.

5. **web-ui/app/api/inventory/sync/route.ts** — 2 calls (lines 23, 61):
   - Already passes `tenantId` as a top-level property in the spread. Verify this is correct — `logResourceAction` spreads all data into `createAuditLog`, so `tenantId` will be in `auditData`. No change needed if confirmed.

**Per D-06, D-07: Verify settings and logo routes (STNG-04, STNG-05)**

6. Read `web-ui/app/api/tenants/settings/route.ts` — confirm GET and PUT both call `getSessionTenantId()` and pass `tenantId` to `TenantSettingsService`. Expected: already correct (GET line 19, PUT line 35). No code change needed.

7. Read `web-ui/app/api/tenants/logo/route.ts` — confirm POST and PUT both call `getSessionTenantId()` and use `tenantId` for S3 key scoping and `TenantSettingsService.saveLogo`. Expected: already correct (POST line 39, PUT line 92). No code change needed.

**Post-fix verification per D-05:**
Run: `grep -rn "logUserAction\|logResourceAction" web-ui/app/api/ | grep -v "tenantId" | grep -v ".test." | grep -v "node_modules"`
This should return 0 lines (all call sites now include tenantId).
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy && grep -rn "logUserAction\|logResourceAction" web-ui/app/api/ | grep -v "tenantId" | grep -v ".test." | grep -v "node_modules" | grep -v "import" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The grep command above returns "0" (all audit call sites include tenantId)
    - grep -q "tenantId" web-ui/app/api/scheduler/execute/route.ts
    - grep -q "tenantId" web-ui/app/api/scheduler/settings/route.ts
    - grep -q "getSessionTenantId" web-ui/app/api/tenants/settings/route.ts (already present, confirmed)
    - grep -q "getSessionTenantId" web-ui/app/api/tenants/logo/route.ts (already present, confirmed)
  </acceptance_criteria>
  <done>All 11 AuditService call sites across web-ui/app/api/ include tenantId as a top-level property. Settings and logo routes confirmed correctly scoped. Zero remaining unscoped audit calls.</done>
</task>

</tasks>

<verification>
1. `grep -rn "getPrismaClient" web-ui/lib/db/repositories/audit-log/postgres.ts` returns 0 lines
2. `grep -rn "getTenantClient" web-ui/lib/db/repositories/audit-log/postgres.ts` returns at least 2 lines (import + usage)
3. `grep -rn "getSessionTenantId" web-ui/app/api/audit/route.ts` returns at least 1 line
4. `grep -rn "logUserAction\|logResourceAction" web-ui/app/api/ | grep -v tenantId | grep -v .test. | grep -v import` returns 0 lines
5. `cd web-ui && npx vitest run lib/db/repositories/audit-log/postgres.test.ts` passes
</verification>

<success_criteria>
- AuditLogPostgresRepository uses getTenantClient(tenantId) exclusively — no getPrismaClient remaining
- /api/audit GET passes tenantId from session to AuditService.getAuditLogs
- /api/audit POST includes tenantId in persisted audit data
- All 11 AuditService.logUserAction/logResourceAction call sites include tenantId
- /api/tenants/settings and /api/tenants/logo confirmed correctly scoped (no changes needed)
</success_criteria>

<output>
After completion, create `.planning/phases/21-audit-settings-regression-tests/21-01-SUMMARY.md`
</output>
