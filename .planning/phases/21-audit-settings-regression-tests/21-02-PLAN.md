---
phase: 21-audit-settings-regression-tests
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - web-ui/lib/db/repositories/account/postgres.test.ts
  - web-ui/lib/db/repositories/schedule/postgres.test.ts
  - web-ui/lib/db/repositories/schedule-execution/postgres.test.ts
  - web-ui/lib/db/repositories/inventory/postgres.test.ts
  - web-ui/lib/db/repositories/agent-ops-run/postgres.test.ts
  - web-ui/lib/db/repositories/agent-ops-event/postgres.test.ts
  - web-ui/lib/db/repositories/scheduled-task/postgres.test.ts
  - web-ui/lib/db/repositories/audit-log/postgres.test.ts
  - web-ui/lib/db/repositories/knowledge-base/postgres.test.ts
  - web-ui/lib/db/repositories/data-source/postgres.test.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Every repository method that touches a tenant-scoped model calls getTenantClient with the correct tenantId"
    - "Tests prove tenantId is present in WHERE clauses for all read, update, and delete operations"
    - "Tests prove tenantId is present in data payload for all create operations"
  artifacts:
    - path: "web-ui/lib/db/repositories/account/postgres.test.ts"
      provides: "Tenant isolation assertions for AccountPostgresRepository"
      contains: "getTenantClient"
    - path: "web-ui/lib/db/repositories/schedule/postgres.test.ts"
      provides: "Tenant isolation assertions for SchedulePostgresRepository"
      contains: "getTenantClient"
    - path: "web-ui/lib/db/repositories/audit-log/postgres.test.ts"
      provides: "Tenant isolation assertions for AuditLogPostgresRepository"
      contains: "getTenantClient"
  key_links:
    - from: "all 10 postgres.test.ts files"
      to: "web-ui/lib/db/pg-config.ts"
      via: "vi.mock getTenantClient"
      pattern: "getTenantClient.*toHaveBeenCalledWith"
---

<objective>
Add tenant isolation assertions to all 10 migrated Postgres repository test files, verifying that getTenantClient is called with the correct tenantId on every method.

Purpose: Lock in TEST-01 — unit-level proof that every repository WHERE clause includes tenantId.
Output: 10 updated test files with getTenantClient mock verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-audit-settings-regression-tests/21-CONTEXT.md
@.planning/phases/21-audit-settings-regression-tests/21-01-SUMMARY.md

<interfaces>
<!-- Mock pattern reference from account/postgres.test.ts -->
<!-- IMPORTANT: After 21-01, audit-log/postgres.ts uses getTenantClient, not getPrismaClient -->
<!-- All 10 repos now use getTenantClient(tenantId) — the mock must match -->

From web-ui/lib/db/pg-config.ts:
```typescript
export function getTenantClient(tenantId: string); // Returns extended Prisma client
export function getPrismaClient(): PrismaClient;
```

Mock pattern (target — what each test file should use):
```typescript
vi.mock('@/lib/db/pg-config', () => ({
    getTenantClient: vi.fn(),
}));
import { getTenantClient } from '@/lib/db/pg-config';

// In beforeEach:
vi.mocked(getTenantClient).mockReturnValue(mockPrisma as any);

// In assertions:
expect(getTenantClient).toHaveBeenCalledWith('tenant-a');
```

Existing test files that already mock getPrismaClient (need migration to getTenantClient):
- web-ui/lib/db/repositories/account/postgres.test.ts — already uses getPrismaClient mock
- web-ui/lib/db/repositories/audit-log/postgres.test.ts — already uses getPrismaClient mock

Repos that already use getTenantClient in production code (Phase 18/19/20 migrations):
- account, schedule, schedule-execution, inventory, agent-ops-run, agent-ops-event,
  scheduled-task, knowledge-base, data-source
- audit-log (after 21-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getTenantClient isolation assertions to repos 1-5 (account, schedule, schedule-execution, inventory, audit-log)</name>
  <files>web-ui/lib/db/repositories/account/postgres.test.ts, web-ui/lib/db/repositories/schedule/postgres.test.ts, web-ui/lib/db/repositories/schedule-execution/postgres.test.ts, web-ui/lib/db/repositories/inventory/postgres.test.ts, web-ui/lib/db/repositories/audit-log/postgres.test.ts</files>
  <read_first>
    - web-ui/lib/db/repositories/account/postgres.test.ts (reference: existing test structure)
    - web-ui/lib/db/repositories/account/postgres.ts (reference: method signatures with tenantId)
    - web-ui/lib/db/repositories/schedule/postgres.test.ts
    - web-ui/lib/db/repositories/schedule/postgres.ts
    - web-ui/lib/db/repositories/schedule-execution/postgres.test.ts
    - web-ui/lib/db/repositories/schedule-execution/postgres.ts
    - web-ui/lib/db/repositories/inventory/postgres.test.ts
    - web-ui/lib/db/repositories/inventory/postgres.ts
    - web-ui/lib/db/repositories/audit-log/postgres.test.ts
    - web-ui/lib/db/repositories/audit-log/postgres.ts
  </read_first>
  <action>
**Per D-08, D-09, D-10: Add tenant isolation test assertions to existing test files**

For each of the 5 test files, apply this pattern:

**Step 1: Update mock from getPrismaClient to getTenantClient (if not already)**

If the test file currently mocks `getPrismaClient`:
```typescript
// BEFORE:
vi.mock('@/lib/db/pg-config', () => ({ getPrismaClient: vi.fn() }));
import { getPrismaClient } from '@/lib/db/pg-config';
// ...
vi.mocked(getPrismaClient).mockReturnValue(mockPrisma as any);

// AFTER:
vi.mock('@/lib/db/pg-config', () => ({ getTenantClient: vi.fn() }));
import { getTenantClient } from '@/lib/db/pg-config';
// ...
vi.mocked(getTenantClient).mockReturnValue(mockPrisma as any);
```

Update ALL existing `vi.mocked(getPrismaClient)` calls to `vi.mocked(getTenantClient)` throughout the file.

**Step 2: Add a `describe('tenant isolation')` block** at the end of each test file (inside the top-level describe or as a new top-level describe). This block should contain one test per public method that verifies `getTenantClient` was called with the correct tenantId:

```typescript
describe('tenant isolation', () => {
    it('[methodName] calls getTenantClient with correct tenantId', async () => {
        // Arrange: set up mock returns
        // Act: call repo method with tenantId='tenant-test'
        // Assert:
        expect(getTenantClient).toHaveBeenCalledWith('tenant-test');
    });
});
```

**Per-repo specifics:**

1. **account/postgres.test.ts** — Methods: getAccounts (tenantId in filters), getAccount, createAccount, updateAccount, deleteAccount. The existing cross-tenant isolation describe block already tests WHERE clause content. Update mock from getPrismaClient to getTenantClient. Add `expect(getTenantClient).toHaveBeenCalledWith(...)` assertions to the existing cross-tenant tests.

2. **schedule/postgres.test.ts** — Methods: getSchedules, getSchedule, createSchedule, updateSchedule, deleteSchedule. Add getTenantClient assertions for each.

3. **schedule-execution/postgres.test.ts** — Methods: getExecutions, logExecution, getExecution. Add getTenantClient assertions for each.

4. **inventory/postgres.test.ts** — Methods: getResources, getResource, upsertResource, deleteResource. Note: upsertResource/upsertBatch may use getPrismaClient for cross-entity lookup — only assert getTenantClient for tenant-scoped operations.

5. **audit-log/postgres.test.ts** — Methods: createAuditLog, getAuditLogs. Update mock from getPrismaClient to getTenantClient. Add `expect(getTenantClient).toHaveBeenCalledWith(...)` assertions.
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy/web-ui && npx vitest run lib/db/repositories/account/postgres.test.ts lib/db/repositories/schedule/postgres.test.ts lib/db/repositories/schedule-execution/postgres.test.ts lib/db/repositories/inventory/postgres.test.ts lib/db/repositories/audit-log/postgres.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getTenantClient" web-ui/lib/db/repositories/account/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/schedule/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/schedule-execution/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/inventory/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/audit-log/postgres.test.ts
    - grep -c "toHaveBeenCalledWith" web-ui/lib/db/repositories/account/postgres.test.ts returns >= 1
    - All 5 test files pass vitest
  </acceptance_criteria>
  <done>5 repository test files mock getTenantClient, verify it is called with the correct tenantId on every public method. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add getTenantClient isolation assertions to repos 6-10 (agent-ops-run, agent-ops-event, scheduled-task, knowledge-base, data-source)</name>
  <files>web-ui/lib/db/repositories/agent-ops-run/postgres.test.ts, web-ui/lib/db/repositories/agent-ops-event/postgres.test.ts, web-ui/lib/db/repositories/scheduled-task/postgres.test.ts, web-ui/lib/db/repositories/knowledge-base/postgres.test.ts, web-ui/lib/db/repositories/data-source/postgres.test.ts</files>
  <read_first>
    - web-ui/lib/db/repositories/agent-ops-run/postgres.test.ts
    - web-ui/lib/db/repositories/agent-ops-run/postgres.ts
    - web-ui/lib/db/repositories/agent-ops-event/postgres.test.ts
    - web-ui/lib/db/repositories/agent-ops-event/postgres.ts
    - web-ui/lib/db/repositories/scheduled-task/postgres.test.ts
    - web-ui/lib/db/repositories/scheduled-task/postgres.ts
    - web-ui/lib/db/repositories/knowledge-base/postgres.test.ts
    - web-ui/lib/db/repositories/knowledge-base/postgres.ts
    - web-ui/lib/db/repositories/data-source/postgres.test.ts
    - web-ui/lib/db/repositories/data-source/postgres.ts
  </read_first>
  <action>
**Per D-08, D-09, D-10: Same pattern as Task 1, applied to repos 6-10**

For each test file:

1. Update mock from `getPrismaClient` to `getTenantClient` if not already done
2. Update all `vi.mocked(getPrismaClient)` to `vi.mocked(getTenantClient)`
3. Add `describe('tenant isolation')` block with one test per public method

**Per-repo specifics:**

6. **agent-ops-run/postgres.test.ts** — Methods: getRuns, getRun, createRun, updateRun. Add getTenantClient assertions.

7. **agent-ops-event/postgres.test.ts** — Methods: getEvents, createEvent. Add getTenantClient assertions.

8. **scheduled-task/postgres.test.ts** — Methods: getTasks, getTask, createTask, updateTask, deleteTask. Add getTenantClient assertions.

9. **knowledge-base/postgres.test.ts** — Methods: getKnowledgeBases, getKnowledgeBase, createKnowledgeBase, updateKnowledgeBase, deleteKnowledgeBase. Add getTenantClient assertions.

10. **data-source/postgres.test.ts** — Methods: getDataSources, getDataSource, createDataSource, updateDataSource, deleteDataSource. Add getTenantClient assertions.

After all 10 files are updated, run the full test suite to confirm no regressions:
```bash
cd web-ui && npx vitest run lib/db/repositories/ --reporter=verbose
```
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy/web-ui && npx vitest run lib/db/repositories/agent-ops-run/postgres.test.ts lib/db/repositories/agent-ops-event/postgres.test.ts lib/db/repositories/scheduled-task/postgres.test.ts lib/db/repositories/knowledge-base/postgres.test.ts lib/db/repositories/data-source/postgres.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getTenantClient" web-ui/lib/db/repositories/agent-ops-run/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/agent-ops-event/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/scheduled-task/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/knowledge-base/postgres.test.ts
    - grep -q "getTenantClient" web-ui/lib/db/repositories/data-source/postgres.test.ts
    - All 5 test files pass vitest
    - Full repo test suite passes: cd web-ui && npx vitest run lib/db/repositories/ (0 failures)
  </acceptance_criteria>
  <done>All 10 repository test files verify getTenantClient is called with the correct tenantId on every public method. Full repository test suite passes with 0 failures.</done>
</task>

</tasks>

<verification>
1. `cd web-ui && npx vitest run lib/db/repositories/ --reporter=verbose` — all 10+ test files pass
2. `grep -rl "getTenantClient" web-ui/lib/db/repositories/*/postgres.test.ts | wc -l` returns 10
3. `grep -rl "getPrismaClient" web-ui/lib/db/repositories/*/postgres.test.ts | wc -l` returns 0 (all migrated)
</verification>

<success_criteria>
- All 10 migrated Postgres repository test files mock getTenantClient (not getPrismaClient)
- Each test file has assertions verifying getTenantClient is called with the correct tenantId for every public method
- Full repository test suite passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/21-audit-settings-regression-tests/21-02-SUMMARY.md`
</output>
