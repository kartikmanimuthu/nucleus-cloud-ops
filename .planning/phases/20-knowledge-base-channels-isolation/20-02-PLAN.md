---
phase: 20-knowledge-base-channels-isolation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/app/api/agent-ops/settings/slack/route.ts
  - web-ui/app/api/agent-ops/settings/jira/route.ts
autonomous: true
requirements: [CHAN-01, CHAN-02, CHAN-03, CHAN-04]

must_haves:
  truths:
    - "User can read Slack channel settings scoped to their tenant only"
    - "User can save Slack channel settings scoped to their tenant only"
    - "User can read Jira channel settings scoped to their tenant only"
    - "User can save Jira channel settings scoped to their tenant only"
  artifacts:
    - path: "web-ui/app/api/agent-ops/settings/slack/route.ts"
      provides: "Tenant-scoped Slack settings GET and PUT"
      contains: "getSessionTenantId"
    - path: "web-ui/app/api/agent-ops/settings/jira/route.ts"
      provides: "Tenant-scoped Jira settings GET and PUT"
      contains: "getSessionTenantId"
  key_links:
    - from: "web-ui/app/api/agent-ops/settings/slack/route.ts"
      to: "web-ui/lib/tenant-config-service.ts"
      via: "TenantConfigService.getConfig(key, tenantId)"
      pattern: "getConfig.*CONFIG_KEY.*tenantId"
    - from: "web-ui/app/api/agent-ops/settings/jira/route.ts"
      to: "web-ui/lib/tenant-config-service.ts"
      via: "TenantConfigService.getConfig(key, tenantId)"
      pattern: "getConfig.*CONFIG_KEY.*tenantId"
    - from: "web-ui/app/api/agent-ops/settings/slack/route.ts"
      to: "web-ui/lib/auth-session.ts"
      via: "getSessionTenantId import"
      pattern: "import.*getSessionTenantId.*from.*auth-session"
    - from: "web-ui/app/api/agent-ops/settings/jira/route.ts"
      to: "web-ui/lib/auth-session.ts"
      via: "getSessionTenantId import"
      pattern: "import.*getSessionTenantId.*from.*auth-session"
---

<objective>
Harden Slack and Jira channel settings routes with tenant isolation.

Purpose: Prevent cross-tenant config leakage — currently both routes call TenantConfigService without tenantId, meaning they read/write to an unscoped config key.
Output: Both channel settings routes extract tenantId from session and pass it to every TenantConfigService call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-knowledge-base-channels-isolation/20-CONTEXT.md

<interfaces>
<!-- TenantConfigService already accepts tenantId — per D-12, no changes needed to the service -->
From web-ui/lib/tenant-config-service.ts:
```typescript
export class TenantConfigService {
    static async getConfig<T = unknown>(configKey: string, tenantId: string): Promise<T | null>;
    static async saveConfig<T = unknown>(configKey: string, data: T, tenantId: string, updatedBy?: string): Promise<void>;
}
```

<!-- Session helper -->
From web-ui/lib/auth-session.ts:
```typescript
export async function getSessionTenantId(): Promise<string>;
```

<!-- Current Slack route — missing tenantId on both getConfig and saveConfig calls -->
From web-ui/app/api/agent-ops/settings/slack/route.ts:
```typescript
// GET: TenantConfigService.getConfig<SlackIntegrationConfig>(CONFIG_KEY)  ← missing tenantId
// PUT: TenantConfigService.saveConfig(CONFIG_KEY, config)                 ← missing tenantId
```

<!-- Current Jira route — same gap -->
From web-ui/app/api/agent-ops/settings/jira/route.ts:
```typescript
// GET: TenantConfigService.getConfig<JiraIntegrationConfig>(CONFIG_KEY)   ← missing tenantId
// PUT: TenantConfigService.saveConfig(CONFIG_KEY, config)                 ← missing tenantId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenantId to Slack and Jira settings routes</name>
  <files>web-ui/app/api/agent-ops/settings/slack/route.ts, web-ui/app/api/agent-ops/settings/jira/route.ts</files>
  <read_first>
    - web-ui/app/api/agent-ops/settings/slack/route.ts (file being modified)
    - web-ui/app/api/agent-ops/settings/jira/route.ts (file being modified)
    - web-ui/lib/tenant-config-service.ts (TenantConfigService signatures — confirm tenantId param position)
    - web-ui/lib/auth-session.ts (getSessionTenantId helper)
  </read_first>
  <action>
    Per D-10 and D-11: Add `getSessionTenantId()` to both Slack and Jira settings routes and pass tenantId to all TenantConfigService calls.

    **Slack route** (web-ui/app/api/agent-ops/settings/slack/route.ts):
    1. Add import: `import { getSessionTenantId } from '@/lib/auth-session';`
    2. GET handler: Add `const tenantId = await getSessionTenantId();` as first line inside try block. Change `TenantConfigService.getConfig<SlackIntegrationConfig>(CONFIG_KEY)` to `TenantConfigService.getConfig<SlackIntegrationConfig>(CONFIG_KEY, tenantId)`.
    3. PUT handler: Add `const tenantId = await getSessionTenantId();` as first line inside try block (before body parsing). Change `TenantConfigService.saveConfig(CONFIG_KEY, config)` to `TenantConfigService.saveConfig(CONFIG_KEY, config, tenantId)`.

    **Jira route** (web-ui/app/api/agent-ops/settings/jira/route.ts):
    1. Add import: `import { getSessionTenantId } from '@/lib/auth-session';`
    2. GET handler: Add `const tenantId = await getSessionTenantId();` as first line inside try block. Change `TenantConfigService.getConfig<JiraIntegrationConfig>(CONFIG_KEY)` to `TenantConfigService.getConfig<JiraIntegrationConfig>(CONFIG_KEY, tenantId)`.
    3. PUT handler: Add `const tenantId = await getSessionTenantId();` as first line inside try block (before body parsing). Change `TenantConfigService.saveConfig(CONFIG_KEY, config)` to `TenantConfigService.saveConfig(CONFIG_KEY, config, tenantId)`.

    Per D-12: TenantConfigService already accepts tenantId — no changes needed to the service itself.
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/multitenancy/web-ui && grep -c "getSessionTenantId" app/api/agent-ops/settings/slack/route.ts app/api/agent-ops/settings/jira/route.ts | grep -v ":0$" | wc -l | xargs test 2 -eq && echo "PASS: both routes have getSessionTenantId" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "getSessionTenantId" in slack/route.ts returns 3 matches (1 import + 2 usages in GET and PUT)
    - grep "getSessionTenantId" in jira/route.ts returns 3 matches (1 import + 2 usages in GET and PUT)
    - grep "getConfig.*CONFIG_KEY)" in both files returns 0 matches (no calls without tenantId — the closing paren after CONFIG_KEY means tenantId is missing)
    - grep "getConfig.*CONFIG_KEY.*tenantId" in both files returns 1 match each
    - grep "saveConfig.*CONFIG_KEY.*tenantId" in both files returns 1 match each
  </acceptance_criteria>
  <done>Both Slack and Jira settings routes extract tenantId from session and pass it to every TenantConfigService.getConfig and saveConfig call. No unscoped config access remains.</done>
</task>

</tasks>

<verification>
1. `grep -r "getSessionTenantId" web-ui/app/api/agent-ops/settings/slack/route.ts web-ui/app/api/agent-ops/settings/jira/route.ts` returns matches in both files
2. `grep "getConfig(CONFIG_KEY)" web-ui/app/api/agent-ops/settings/slack/route.ts web-ui/app/api/agent-ops/settings/jira/route.ts` returns zero matches (no unscoped calls)
3. `grep "saveConfig(CONFIG_KEY," web-ui/app/api/agent-ops/settings/slack/route.ts web-ui/app/api/agent-ops/settings/jira/route.ts` shows tenantId in all calls
4. `cd web-ui && npx tsc --noEmit` compiles without errors
</verification>

<success_criteria>
- Slack GET/PUT pass tenantId to TenantConfigService
- Jira GET/PUT pass tenantId to TenantConfigService
- No unscoped TenantConfigService calls remain in either file
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-knowledge-base-channels-isolation/20-02-SUMMARY.md`
</output>
