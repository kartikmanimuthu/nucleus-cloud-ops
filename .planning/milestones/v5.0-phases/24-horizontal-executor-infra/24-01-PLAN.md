---
phase: 24-horizontal-executor-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/src/executor/horizontal.ts
  - workers/src/executor/horizontal.test.ts
autonomous: true
requirements: [EXEC-04]

must_haves:
  truths:
    - "HorizontalExecutor.execute() calls ECS RunTask with correct cluster, task def, subnets, security group from env vars"
    - "Container command override passes job name and serialized data to job-runner.js"
    - "Executor polls DescribeTasks until STOPPED, returns on exit code 0, throws on non-zero"
    - "RunTask failure (capacity, permissions) throws immediately without polling"
    - "Timeout after HORIZONTAL_TASK_TIMEOUT_MS (default 15 min) throws an error"
  artifacts:
    - path: "workers/src/executor/horizontal.ts"
      provides: "ECS RunTask dispatch with polling"
      exports: ["HorizontalExecutor"]
    - path: "workers/src/executor/horizontal.test.ts"
      provides: "Unit tests for HorizontalExecutor"
      min_lines: 80
  key_links:
    - from: "workers/src/executor/horizontal.ts"
      to: "@aws-sdk/client-ecs"
      via: "ECSClient, RunTaskCommand, DescribeTasksCommand"
      pattern: "new ECSClient"
    - from: "workers/src/executor/horizontal.ts"
      to: "workers/src/executor/types.ts"
      via: "implements JobExecutor"
      pattern: "implements JobExecutor"
---

<objective>
Replace the HorizontalExecutor stub with a full ECS RunTask dispatch implementation that launches ephemeral Fargate tasks and polls for completion.

Purpose: This is the core runtime logic that makes WORKER_ARCH=horizontal actually dispatch jobs to separate ECS tasks instead of running them in-process.
Output: Working HorizontalExecutor class with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-horizontal-executor-infra/24-CONTEXT.md

@workers/src/executor/types.ts
@workers/src/executor/horizontal.ts
@workers/src/executor/vertical.ts
@workers/src/executor/factory.ts
@workers/src/job-runner.ts
@workers/package.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From workers/src/executor/types.ts:
```typescript
export type HandlerFn = (jobData: unknown) => Promise<void>;

export interface JobExecutor {
    execute(jobName: string, jobData: unknown): Promise<void>;
    registerHandler?(jobName: string, handler: HandlerFn): void;
}
```

From workers/src/lib/logger.ts:
```typescript
export function createLogger(service: string): Logger;
// Logger has .info(), .warn(), .error(), .debug() methods
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement HorizontalExecutor ECS RunTask dispatch with polling</name>
  <files>workers/src/executor/horizontal.ts, workers/src/executor/horizontal.test.ts</files>
  <read_first>
    - workers/src/executor/horizontal.ts (current stub to replace)
    - workers/src/executor/types.ts (JobExecutor interface contract)
    - workers/src/executor/vertical.ts (reference implementation pattern)
    - workers/src/job-runner.ts (CLI args format: --job <name> --data '<json>')
    - workers/package.json (confirm @aws-sdk/client-ecs already in dependencies)
  </read_first>
  <behavior>
    - Test 1: execute() calls RunTaskCommand with cluster=HORIZONTAL_CLUSTER_ARN, taskDefinition=HORIZONTAL_TASK_DEF_ARN, subnets from HORIZONTAL_SUBNETS (comma-split), securityGroups=[HORIZONTAL_SECURITY_GROUP], assignPublicIp="DISABLED", launchType="FARGATE"
    - Test 2: containerOverrides.command = ["node", "dist/job-runner.js", "--job", jobName, "--data", JSON.stringify(jobData)]
    - Test 3: after RunTask succeeds, polls DescribeTasksCommand until task.lastStatus === "STOPPED"
    - Test 4: exit code 0 (task.containers[0].exitCode === 0) → resolves successfully
    - Test 5: exit code non-zero → throws error with exit code and task ARN
    - Test 6: RunTask returns empty tasks array or failures array → throws immediately, no polling
    - Test 7: polling exceeds HORIZONTAL_TASK_TIMEOUT_MS (default 900000) → throws timeout error
    - Test 8: missing env vars (HORIZONTAL_CLUSTER_ARN etc.) → throws descriptive error on execute()
    - Test 9: exponential backoff — first poll at ~2s, doubles up to 30s cap
  </behavior>
  <action>
    Replace the stub in `workers/src/executor/horizontal.ts` with full implementation per D-01 through D-15:

    1. Import `ECSClient`, `RunTaskCommand`, `DescribeTasksCommand` from `@aws-sdk/client-ecs` (already in workers/package.json).

    2. Read config from env vars in constructor or at execute() time:
       - `HORIZONTAL_CLUSTER_ARN` (required)
       - `HORIZONTAL_TASK_DEF_ARN` (required)
       - `HORIZONTAL_SUBNETS` (required, comma-separated string → split into string[])
       - `HORIZONTAL_SECURITY_GROUP` (required)
       - `HORIZONTAL_TASK_TIMEOUT_MS` (optional, default `900000`)
       - `AWS_REGION` (optional, ECSClient picks up default)
       Throw a clear error if any required var is missing when execute() is called.

    3. `execute(jobName: string, jobData: unknown): Promise<void>`:
       a. Build RunTaskCommand:
          ```typescript
          {
            cluster: clusterArn,
            taskDefinition: taskDefArn,
            launchType: "FARGATE",
            networkConfiguration: {
              awsvpcConfiguration: {
                subnets: subnets,
                securityGroups: [securityGroup],
                assignPublicIp: "DISABLED",
              },
            },
            overrides: {
              containerOverrides: [{
                name: "WorkersContainer",
                command: ["node", "dist/job-runner.js", "--job", jobName, "--data", JSON.stringify(jobData)],
              }],
            },
          }
          ```
       b. Call `ecsClient.send(runTaskCmd)`. If `result.failures` is non-empty or `result.tasks` is empty, throw immediately with failure reason.
       c. Extract `taskArn` from `result.tasks[0].taskArn`.
       d. Poll with exponential backoff (initial 2000ms, factor 2x, cap 30000ms):
          - Call `DescribeTasksCommand({ cluster: clusterArn, tasks: [taskArn] })`
          - If `task.lastStatus === "STOPPED"`, check `task.containers[0].exitCode`
          - Exit code 0 → return (success)
          - Exit code non-zero → throw error with exit code, task ARN, and stoppedReason
          - If not STOPPED, sleep and retry with increased interval
          - If total elapsed time exceeds `timeoutMs`, throw timeout error
       e. Log at info level: dispatch start (jobName, taskArn), completion (exitCode), and errors.

    4. Do NOT implement `registerHandler` — HorizontalExecutor dispatches to ECS, it doesn't run handlers locally.

    5. Create `workers/src/executor/horizontal.test.ts`:
       - Mock `@aws-sdk/client-ecs` using vitest `vi.mock()`
       - Set env vars in `beforeEach`, clean in `afterEach`
       - Use `vi.useFakeTimers()` for timeout tests
       - Test all 9 behaviors listed above
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/pg-boss-migration/workers && npx vitest run src/executor/horizontal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - workers/src/executor/horizontal.ts contains `import { ECSClient, RunTaskCommand, DescribeTasksCommand }` from `@aws-sdk/client-ecs`
    - workers/src/executor/horizontal.ts contains `class HorizontalExecutor implements JobExecutor`
    - workers/src/executor/horizontal.ts contains `async execute(jobName: string, jobData: unknown): Promise<void>`
    - workers/src/executor/horizontal.ts contains `launchType: "FARGATE"`
    - workers/src/executor/horizontal.ts contains `"node", "dist/job-runner.js", "--job"`
    - workers/src/executor/horizontal.ts contains `assignPublicIp: "DISABLED"`
    - workers/src/executor/horizontal.ts contains `HORIZONTAL_CLUSTER_ARN`
    - workers/src/executor/horizontal.ts contains `HORIZONTAL_TASK_DEF_ARN`
    - workers/src/executor/horizontal.ts contains `HORIZONTAL_SUBNETS`
    - workers/src/executor/horizontal.ts contains `HORIZONTAL_SECURITY_GROUP`
    - workers/src/executor/horizontal.ts contains `lastStatus` (polling check)
    - workers/src/executor/horizontal.ts contains `exitCode` (completion check)
    - workers/src/executor/horizontal.test.ts exists with at least 8 test cases
    - `cd workers && npx vitest run src/executor/horizontal.test.ts` exits 0
  </acceptance_criteria>
  <done>HorizontalExecutor dispatches ECS RunTask with correct config, polls for completion, handles success/failure/timeout. All unit tests pass.</done>
</task>

</tasks>

<verification>
- `cd workers && npx vitest run src/executor/horizontal.test.ts` — all tests pass
- `cd workers && npx tsc --noEmit` — no type errors
- `grep -c "FARGATE" workers/src/executor/horizontal.ts` returns at least 1
- `grep -c "DescribeTasksCommand" workers/src/executor/horizontal.ts` returns at least 1
</verification>

<success_criteria>
HorizontalExecutor is a complete ECS RunTask dispatcher: reads config from env vars, launches Fargate tasks with job-runner command override, polls until STOPPED, returns on exit 0, throws on non-zero or timeout. All unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/24-horizontal-executor-infra/24-01-SUMMARY.md`
</output>
