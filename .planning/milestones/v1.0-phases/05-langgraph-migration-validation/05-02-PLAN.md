---
phase: 05-langgraph-migration-validation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web-ui/lib/agent/persistence.ts
  - web-ui/lib/agent/persistence.test.ts
autonomous: true
requirements: [LANG-01, LANG-02, LANG-03, LANG-04, LANG-06]
must_haves:
  truths:
    - "persistence.ts uses PostgresSaver when USE_PG_LANGGRAPH=true"
    - "persistence.ts uses DynamoDB persistence when USE_PG_LANGGRAPH=false"
    - "getCheckpointer() returns PostgresSaver instance with PG flag on"
    - "getChatHistory() returns PostgreSQL-backed chat history with PG flag on"
    - "getMemoryStore() returns pgvector-backed memory store with PG flag on"
    - "saveMemory() and searchMemory() work with PostgreSQL backend"
    - "Public API unchanged — callers (fast-agent, planning-agent, deep-agent, model-factory, executor-graphs, chat route, threads route) need zero changes"
  artifacts:
    - path: "web-ui/lib/agent/persistence.ts"
      provides: "Feature-flagged persistence with PostgreSQL + DynamoDB backends"
      exports: ["getCheckpointer", "getMemoryStore", "getChatHistory", "saveMemory", "searchMemory"]
    - path: "web-ui/lib/agent/persistence.test.ts"
      provides: "TDD unit tests for persistence module"
      min_lines: 50
  key_links:
    - from: "web-ui/lib/agent/persistence.ts"
      to: "@langchain/langgraph-checkpoint-postgres"
      via: "PostgresSaver import and setup()"
      pattern: "PostgresSaver"
    - from: "web-ui/lib/agent/persistence.ts"
      to: "web-ui/lib/db/pg-config.ts"
      via: "DATABASE_URL for PostgreSQL connection"
      pattern: "DATABASE_URL"
---

<objective>
Rewrite persistence.ts to support both DynamoDB and PostgreSQL backends via USE_PG_LANGGRAPH feature flag, preserving the identical public API. Add TDD unit tests.

Purpose: This is the core migration — all agent files import from persistence.ts. The feature flag enables instant rollback.
Output: Updated persistence.ts with dual-backend support, unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-langgraph-migration-validation/05-CONTEXT.md
@.planning/phases/05-langgraph-migration-validation/05-01-SUMMARY.md

@web-ui/lib/agent/persistence.ts
@web-ui/lib/db/pg-config.ts
@web-ui/lib/agent/agent-shared.ts
@web-ui/lib/agent/model-factory.ts

<interfaces>
<!-- Current persistence.ts public API — MUST be preserved exactly -->

From web-ui/lib/agent/persistence.ts:
```typescript
export async function getCheckpointer(): Promise<DynamoDBSaver>
export async function getMemoryStore(): Promise<DynamoDBStore>
export async function getChatHistory(): Promise<DynamoDBChatMessageHistory>
export async function saveMemory(userId: string, namespace: string[], key: string, value: Record<string, unknown>): Promise<void>
export async function searchMemory(userId: string, namespacePrefix: string[], query: string, limit?: number): Promise<unknown[]>
```

From web-ui/lib/agent/agent-shared.ts (line 5):
```typescript
import { getCheckpointer as getDynamoCheckpointer, getMemoryStore } from "./persistence";
```

From web-ui/lib/agent/model-factory.ts (line 25):
```typescript
import { saveMemory, searchMemory } from "./persistence";
```

Callers that import from persistence.ts:
- web-ui/lib/agent/agent-shared.ts → getCheckpointer, getMemoryStore
- web-ui/lib/agent/model-factory.ts → saveMemory, searchMemory
- web-ui/lib/agent/planning-agent.ts → getCheckpointer (via agent-shared re-export)
- web-ui/lib/agent/fast-agent.ts → getCheckpointer (via agent-shared re-export)
- web-ui/lib/agent/deep-agent.ts → getCheckpointer (via agent-shared re-export)
- web-ui/lib/agent-ops/executor-graphs.ts → getCheckpointer, getMemoryStore
- web-ui/app/api/chat/route.ts → getChatHistory
- web-ui/app/api/threads/[threadId]/history/route.ts → getCheckpointer, getChatHistory
- web-ui/app/api/threads/[threadId]/route.ts → getChatHistory
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD unit tests for persistence.ts PostgreSQL backend</name>
  <files>web-ui/lib/agent/persistence.test.ts</files>
  <read_first>
    - web-ui/lib/agent/persistence.ts (current implementation — understand public API)
    - web-ui/lib/db/pg-config.ts (getPrismaClient pattern)
    - node_modules/@langchain/langgraph-checkpoint-postgres/README.md or package.json (confirm PostgresSaver API — does it have setup()? What constructor args?)
  </read_first>
  <behavior>
    - Test 1: getCheckpointer() returns DynamoDBSaver when USE_PG_LANGGRAPH is unset/false
    - Test 2: getCheckpointer() returns PostgresSaver when USE_PG_LANGGRAPH=true
    - Test 3: getChatHistory() returns DynamoDB-backed history when USE_PG_LANGGRAPH is false
    - Test 4: getChatHistory() returns PG-backed history when USE_PG_LANGGRAPH=true
    - Test 5: getMemoryStore() returns DynamoDBStore when USE_PG_LANGGRAPH is false
    - Test 6: getMemoryStore() returns PG-backed store when USE_PG_LANGGRAPH=true
    - Test 7: Singleton pattern — calling getCheckpointer() twice returns same instance
    - Test 8: Public API exports all 5 functions (getCheckpointer, getMemoryStore, getChatHistory, saveMemory, searchMemory)
  </behavior>
  <action>
    Create `web-ui/lib/agent/persistence.test.ts` with Vitest tests.

    Mock the external dependencies:
    - Mock `@farukada/aws-langgraph-dynamodb-ts` (DynamoDBSaver, DynamoDBStore, DynamoDBChatMessageHistory)
    - Mock `@langchain/langgraph-checkpoint-postgres` (PostgresSaver)
    - Mock `@langchain/aws` (BedrockEmbeddings)
    - Mock `@/lib/db/pg-config` (getPrismaClient)

    Use `vi.stubEnv()` to toggle `USE_PG_LANGGRAPH` between tests.
    Use `beforeEach` to clear globalThis singleton cache between tests.

    Tests should verify the return type/instance, not deep implementation details.
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/web-ui && npx vitest run lib/agent/persistence.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "describe.*persistence" web-ui/lib/agent/persistence.test.ts returns a match
    - grep "USE_PG_LANGGRAPH" web-ui/lib/agent/persistence.test.ts returns a match
    - grep "PostgresSaver" web-ui/lib/agent/persistence.test.ts returns a match
    - grep "DynamoDBSaver" web-ui/lib/agent/persistence.test.ts returns a match
    - vitest run lib/agent/persistence.test.ts exits with code 0
  </acceptance_criteria>
  <done>All 8 persistence unit tests pass, covering both DynamoDB and PostgreSQL code paths</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite persistence.ts with USE_PG_LANGGRAPH feature flag</name>
  <files>web-ui/lib/agent/persistence.ts</files>
  <read_first>
    - web-ui/lib/agent/persistence.ts (current full implementation)
    - web-ui/lib/agent/persistence.test.ts (tests from Task 1 — implement to pass these)
    - web-ui/lib/db/pg-config.ts (getPrismaClient for raw SQL queries)
    - prisma/schema.prisma (AgentMemory and ChatMessage models from 05-01)
    - node_modules/@langchain/langgraph-checkpoint-postgres/dist/index.d.ts or README (PostgresSaver constructor signature, setup() method)
  </read_first>
  <action>
    Rewrite `web-ui/lib/agent/persistence.ts` per D-01, D-02, D-03, D-04:

    1. Keep ALL existing DynamoDB code paths intact (they run when USE_PG_LANGGRAPH !== 'true').

    2. Add PostgreSQL code path (when USE_PG_LANGGRAPH === 'true'):

    **Checkpointer (LANG-01):**
    ```typescript
    import { PostgresSaver } from "@langchain/langgraph-checkpoint-postgres";
    ```
    - Create PostgresSaver with `DATABASE_URL` connection string
    - Call `await saver.setup()` to auto-create checkpoint tables (the library manages its own schema)
    - Return the saver instance from getCheckpointer()

    **Chat History (LANG-02):**
    - Implement a `PostgresChatHistory` class that uses Prisma's ChatMessage model
    - Methods: addMessage(sessionId, role, content), getMessages(sessionId), clearMessages(sessionId)
    - Set expiresAt to now + 30 days on each insert
    - Return from getChatHistory()
    - The class must be compatible with how callers use it (check chat/route.ts usage patterns)

    **Memory Store (LANG-03):**
    - Implement a `PostgresMemoryStore` class that uses Prisma's AgentMemory model + raw SQL for pgvector
    - `batch()` for put operations: upsert into agent_memories with embedding from BedrockEmbeddings
    - `batch()` for search operations: raw SQL `SELECT ... ORDER BY embedding <=> $1 LIMIT $2` (cosine distance per D-06)
    - Set expiresAt to now + 90 days on each insert (per D-07)
    - Return from getMemoryStore()

    3. Preserve the globalThis singleton pattern (survives Next.js hot reloads).

    4. Return types for the public API functions should use a union or base type so callers don't need to change:
    - `getCheckpointer()` → return type should be `BaseCheckpointSaver` (from `@langchain/langgraph-checkpoint`) — agent-shared.ts already casts to this
    - `getMemoryStore()` → return type can be the store interface (DynamoDBStore | PostgresMemoryStore)
    - `getChatHistory()` → return type can be the history interface

    5. `saveMemory()` and `searchMemory()` helper functions must work with both backends — they delegate to getMemoryStore() which handles the switch.

    6. Feature flag check: `const usePg = process.env.USE_PG_LANGGRAPH === 'true';`
  </action>
  <verify>
    <automated>cd /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/web-ui && npx vitest run lib/agent/persistence.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "USE_PG_LANGGRAPH" web-ui/lib/agent/persistence.ts returns a match
    - grep "PostgresSaver" web-ui/lib/agent/persistence.ts returns a match
    - grep "getCheckpointer" web-ui/lib/agent/persistence.ts returns a match
    - grep "getMemoryStore" web-ui/lib/agent/persistence.ts returns a match
    - grep "getChatHistory" web-ui/lib/agent/persistence.ts returns a match
    - grep "saveMemory" web-ui/lib/agent/persistence.ts returns a match
    - grep "searchMemory" web-ui/lib/agent/persistence.ts returns a match
    - grep "globalThis" web-ui/lib/agent/persistence.ts returns a match (singleton preserved)
    - grep "DynamoDBSaver" web-ui/lib/agent/persistence.ts returns a match (DynamoDB path preserved)
    - vitest run lib/agent/persistence.test.ts exits with code 0
  </acceptance_criteria>
  <done>persistence.ts supports both DynamoDB and PostgreSQL backends via USE_PG_LANGGRAPH flag, all unit tests pass, public API unchanged</done>
</task>

</tasks>

<verification>
- `cd web-ui && npx vitest run lib/agent/persistence.test.ts` — all tests pass
- `grep -c "export async function" web-ui/lib/agent/persistence.ts` returns 5 (getCheckpointer, getMemoryStore, getChatHistory, saveMemory, searchMemory)
- No changes needed in any caller file (fast-agent.ts, planning-agent.ts, deep-agent.ts, model-factory.ts, executor-graphs.ts, chat/route.ts, threads routes)
</verification>

<success_criteria>
- persistence.ts feature-flagged with USE_PG_LANGGRAPH, both backends work, all TDD tests pass, zero caller changes required
</success_criteria>

<output>
After completion, create `.planning/phases/05-langgraph-migration-validation/05-02-SUMMARY.md`
</output>
