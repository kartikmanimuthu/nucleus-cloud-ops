---
phase: 01-foundation-tenant-config
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/.env.local.example
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FOUND-03
  - TCFG-07
  - MIGR-01
  - MIGR-02
  - MIGR-06
gap_closure: true

must_haves:
  truths:
    - ".env.local.example DATABASE_URL includes ?connection_limit=10"
    - ".env.local.example has a comment about Lambda connection_limit=3"
    - "REQUIREMENTS.md shows TCFG-07, MIGR-01, MIGR-02, MIGR-06 as Complete"
  artifacts:
    - path: "web-ui/.env.local.example"
      provides: "DATABASE_URL with connection_limit query parameter"
      contains: "connection_limit=10"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability for 4 requirements"
      contains: "Complete"
  key_links: []
---

<objective>
Fix the DATABASE_URL connection_limit gap in .env.local.example and update REQUIREMENTS.md traceability for 4 implemented-but-unstamped requirements.

Purpose: FOUND-03 requires pool sizes to be configured (not just documented). The .env.local.example DATABASE_URL is missing `?connection_limit=10`. Additionally, TCFG-07, MIGR-01, MIGR-02, MIGR-06 are all verified as implemented but REQUIREMENTS.md still shows them as Pending.

Output: Updated .env.local.example and REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-tenant-config/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add connection_limit to DATABASE_URL in .env.local.example</name>
  <read_first>
    - web-ui/.env.local.example (current file — must see lines 77-87 for PostgreSQL section)
    - web-ui/lib/db/pg-config.ts (to confirm how Prisma uses the DATABASE_URL)
  </read_first>
  <files>web-ui/.env.local.example</files>
  <action>
Edit web-ui/.env.local.example to make two changes in the PostgreSQL section (around line 77-78):

1. Change line 78 from:
   ```
   DATABASE_URL=postgresql://nucleus:nucleus_dev@localhost:5432/nucleus
   ```
   to:
   ```
   DATABASE_URL=postgresql://nucleus:nucleus_dev@localhost:5432/nucleus?connection_limit=10
   ```

2. Add a comment line immediately after the DATABASE_URL line:
   ```
   # Lambda functions should use ?connection_limit=3 in their own DATABASE_URL
   ```

The resulting block should read:
```
# PostgreSQL (local dev via Docker Compose)
DATABASE_URL=postgresql://nucleus:nucleus_dev@localhost:5432/nucleus?connection_limit=10
# Lambda functions should use ?connection_limit=3 in their own DATABASE_URL
```

Do NOT modify any other lines in the file. Per FOUND-03: "PostgreSQL connection singleton with pool sizes: max 10 for ECS, max 3 for Lambda."
  </action>
  <verify>
    <automated>grep -c "connection_limit=10" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/web-ui/.env.local.example && grep -c "connection_limit=3" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/web-ui/.env.local.example</automated>
  </verify>
  <acceptance_criteria>
    - web-ui/.env.local.example line containing DATABASE_URL ends with `?connection_limit=10`
    - web-ui/.env.local.example contains the comment `# Lambda functions should use ?connection_limit=3 in their own DATABASE_URL`
    - All other lines in .env.local.example are unchanged (USE_PG_* flags still present, all other config intact)
  </acceptance_criteria>
  <done>DATABASE_URL in .env.local.example includes ?connection_limit=10 and a Lambda comment is present. FOUND-03 is fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability for TCFG-07, MIGR-01, MIGR-02, MIGR-06</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current file — must see lines 25-26 for TCFG-07 checkbox, and lines 94-99/143/194-199 for traceability table)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make these specific edits in .planning/REQUIREMENTS.md:

**Edit 1 — TCFG-07 checkbox (line 25):**
Change:
```
- [ ] **TCFG-07**: Data migration script seeds tenant configs from DynamoDB (AWS_PROFILE=PLATFORM-ADMIN)
```
to:
```
- [x] **TCFG-07**: Data migration script seeds tenant configs from DynamoDB (AWS_PROFILE=PLATFORM-ADMIN)
```

**Edit 2 — MIGR-01 checkbox (line 94):**
Change:
```
- [ ] **MIGR-01**: Each migration script uses AWS_PROFILE=PLATFORM-ADMIN for DynamoDB access
```
to:
```
- [x] **MIGR-01**: Each migration script uses AWS_PROFILE=PLATFORM-ADMIN for DynamoDB access
```

**Edit 3 — MIGR-02 checkbox (line 95):**
Change:
```
- [ ] **MIGR-02**: All scripts are idempotent (ON CONFLICT DO UPDATE) for safe re-runs
```
to:
```
- [x] **MIGR-02**: All scripts are idempotent (ON CONFLICT DO UPDATE) for safe re-runs
```

**Edit 4 — MIGR-06 checkbox (line 99):**
Change:
```
- [ ] **MIGR-06**: Progress logging shows "Migrated X/Y records..." during execution
```
to:
```
- [x] **MIGR-06**: Progress logging shows "Migrated X/Y records..." during execution
```

**Edit 5 — Traceability table TCFG-07 row (line 143):**
Change:
```
| TCFG-07 | Phase 1 | Pending |
```
to:
```
| TCFG-07 | Phase 1 | Complete |
```

**Edit 6 — Traceability table MIGR-01 row (line 194):**
Change:
```
| MIGR-01 | Phase 1 | Pending |
```
to:
```
| MIGR-01 | Phase 1 | Complete |
```

**Edit 7 — Traceability table MIGR-02 row (line 195):**
Change:
```
| MIGR-02 | Phase 1 | Pending |
```
to:
```
| MIGR-02 | Phase 1 | Complete |
```

**Edit 8 — Traceability table MIGR-06 row (line 199):**
Change:
```
| MIGR-06 | Phase 1 | Pending |
```
to:
```
| MIGR-06 | Phase 1 | Complete |
```

**Edit 9 — Last updated line (line 208):**
Change:
```
*Last updated: 2026-03-26 after roadmap creation — traceability confirmed 62/62*
```
to:
```
*Last updated: 2026-03-27 after phase 01 verification — TCFG-07, MIGR-01, MIGR-02, MIGR-06 marked complete*
```

Do NOT change any other requirements or traceability rows.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*TCFG-07\*\*" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/.planning/REQUIREMENTS.md && grep -c "\[x\] \*\*MIGR-01\*\*" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/.planning/REQUIREMENTS.md && grep -c "\[x\] \*\*MIGR-02\*\*" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/.planning/REQUIREMENTS.md && grep -c "\[x\] \*\*MIGR-06\*\*" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/.planning/REQUIREMENTS.md && grep "TCFG-07.*Complete" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/.planning/REQUIREMENTS.md && grep "MIGR-06.*Complete" /Users/kartik/.superset/worktrees/nucleus-cloud-ops/database-migration/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `[x] **TCFG-07**` (not `[ ]`)
    - REQUIREMENTS.md contains `[x] **MIGR-01**` (not `[ ]`)
    - REQUIREMENTS.md contains `[x] **MIGR-02**` (not `[ ]`)
    - REQUIREMENTS.md contains `[x] **MIGR-06**` (not `[ ]`)
    - Traceability table row for TCFG-07 shows `Complete` (not `Pending`)
    - Traceability table row for MIGR-01 shows `Complete` (not `Pending`)
    - Traceability table row for MIGR-02 shows `Complete` (not `Pending`)
    - Traceability table row for MIGR-06 shows `Complete` (not `Pending`)
    - No other requirement checkboxes or traceability rows were modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects TCFG-07, MIGR-01, MIGR-02, MIGR-06 as complete with updated traceability table.</done>
</task>

</tasks>

<verification>
- `grep "connection_limit=10" web-ui/.env.local.example` matches the DATABASE_URL line
- `grep "connection_limit=3" web-ui/.env.local.example` matches the Lambda comment
- `grep -c "\[x\]" .planning/REQUIREMENTS.md` shows the correct count (increased by 4)
- `grep "Pending" .planning/REQUIREMENTS.md` no longer shows TCFG-07, MIGR-01, MIGR-02, MIGR-06
</verification>

<success_criteria>
1. .env.local.example DATABASE_URL includes `?connection_limit=10` and has Lambda connection_limit=3 comment
2. REQUIREMENTS.md shows TCFG-07, MIGR-01, MIGR-02, MIGR-06 as [x] Complete in both checkbox and traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tenant-config/01-07-SUMMARY.md`
</output>
