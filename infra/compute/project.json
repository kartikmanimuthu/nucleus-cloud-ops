{
  "name": "compute",
  "projectType": "application",
  "targets": {
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pulumi preview --stack {args.stack}",
        "cwd": "infra/compute"
      }
    },
    "up": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pulumi up --stack {args.stack} --yes",
        "cwd": "infra/compute"
      }
    },
    "destroy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pulumi destroy --stack {args.stack}",
        "cwd": "infra/compute"
      }
    }
  }
}
